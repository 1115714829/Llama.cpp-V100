# HANDOFF-2026-09-22.md — 换设备/换会话接手包（读这一份就够）

> 生成时间：2026-09-22 10:05（服务器本地盘 `/mnt/3.84t/v100-opt/kernel-lab/` 与本工作区同步）
> **本文件是"今天这一轮的交接"**；长期规则看 `AGENTS.md`（自动加载）与 `QWEN.md`；图看 `PLAN-GRAPH.md`；账看 `BASELINE-LEDGER.md`。

---

## 0. 30 秒状态

| 项 | 值 |
|---|---|
| 目标（不变） | `llama.cpp` 在 V100 上追平 1cat-vLLM；**硬指标 tg ≥ 150 t/s**（AL 5.55 下 ms/轮 ≤ 37.0） |
| 当前基准 | **B3**：tg 102.09 / 89.22 / 130.72，AL 5.55/4.22/6.38，**ms/轮 54.36/47.44/48.48（均值 50.22）**，门 `f3edac19…` |
| 今天新增的**唯一大发现** | **预填充的墙 = 长 KV 的 `FLASH_ATTN_EXT`**：`D=256, n_q=2048, n_kv=262144` 单算子 **368,757 µs = 预填充算子总时间的 78.4%**，只跑到 **1.49 TFLOPS（V100 FP16 峰值 125 的 1.2%）**；除 LM head 外**全部 MUL_MAT 合计仅 4.3%** |
| 下一手 | **FA-D256**（图上金色节点）：FA 内核本身，落在**预填充侧 = V100 真正支持的硬件支路（FP16 张量核）** |
| 今天被否证的 4 件事 | 解码侧 mma 内核、MMVQ VDR=8、sched 快路径重试、`rows_per_block`；以及"整条 KV 每 ubatch 反量化"这个假设 —— **别再重做**（见 §5） |

---

## 1. ★ 复制粘贴区（新设备上给 agent 的第一条消息）

```
接手 llama.cpp V100 专项优化项目。先按顺序读：
1) 工作区根 AGENTS.md（自动加载，含红线与度量纪律）
2) 1cat-vllm-v100-study/HANDOFF-2026-09-22.md（今天这一轮的交接，先读 §0/§4/§5/§6）
3) 1cat-vllm-v100-study/PLAN-GRAPH.md 的 §1 主图（看金色节点 FA-D256 与红色节点 FA78）
4) 1cat-vllm-v100-study/BASELINE-LEDGER.md 的最新几轮 R258–R265
5) 1cat-vllm-v100-study/HANDOFF.md 与 AUDIT-2026-09-20-dsh.md（长期实况与可信度索引）

今天的结论一句话：预填充的墙不是 GEMM、也不是 KV 反量化，而是长 KV 的 FLASH_ATTN_EXT
（n_kv=262144 时单算子 368.8 ms = 78.4%，只有 1.49 TFLOPS）。

下一步请做 FA-D256：
  (a) 先用现成探针看清我们走哪个内核：
      GGML_CUDA_FA_KERNEL_DEBUG=1 llama-bench -m /mnt/3.84t/Qwen3.8-27B-GGUF/Qwen3.8-27B-Q8_0.gguf \
        -p 32768 -n 0 -r 2 -ub 2048 -ts 1/1/1 --flash-attn 1 -ctk q8_0 -ctv q8_0
      （CUDA_VISIBLE_DEVICES=0,1,2、GGML_CUDA_P2P=1 必须显式设）
      看 [FAK] 行：kernel= / D= / n_q= / n_kv= / kv_type= / need_f16_K/V=
  (b) 读 ggml/src/ggml-cuda/fattn.cu 的 ggml_cuda_get_best_fattn_kernel 与 D=256 的 tile 配置
      （怀疑 n_kv 很大时 split-KV/combine 路径在吃时间，先证伪或证实）
  (c) 参考实现（本地只读）：v100-refs/jusko-llama-volta-qwen3flash/ggml/src/ggml-cuda/fattn-q8-volta.cuh
      以及图上 JS2/JS3/JS4（JS4 的 2-CTA 门控正好是 gqa_ratio==6 && D==256 = 我们的形状）

红线与纪律（必须遵守）：
- 只在 /root/llm/test/v100-opt/llama.cpp 改码；/mnt/3.84t/v100-opt/llama.cpp 是陈旧副本
- 构建后必须 strings <lib> | grep -c <标记串> 证明代码进了被加载的库
- 跑臂必须显式 L=/root/libdir-instr（harness 默认值指向过期库）
- 不要代我 commit / push / 开 PR
```

---

## 2. 换设备要复制的文件（按优先级）

### 必带（小，缺了接不上）
| 路径 | 大小 | 说明 |
|---|---|---|
| `AGENTS.md`（工作区根） | ~65 KB | 每次会话自动加载；红线、度量纪律、环境边界 |
| `QWEN.md`（工作区根） | ~60 KB | 持久规则全文 + **服务器登录信息**（⚠️ 不要提交/外传） |
| `1cat-vllm-v100-study/` 整目录 | 41 MB | 研究档案；**核心**：`HANDOFF-2026-09-22.md`(本文件)、`PLAN-GRAPH.md`、`BASELINE-LEDGER.md`、`HANDOFF.md`、`AUDIT-2026-09-20-dsh.md`、`E4/E5/E6/E7/E8/E9-*.md`、`DESIGN-W8VOLTA-MMA.md` |
| `1cat-vllm-v100-study/kernel-lab/` | <1 MB | 今天的实验台与脚本（mma 内核、预填充测量、解析器） |
| `1cat-vllm-v100-study/wip-*.py` / `wip-*.patch` | 小 | 各轮补丁脚本与存档（含 `wip-local-uncommitted.patch` = 本地唯一未提交改动） |
| `llama.cpp/`（源码树） | 166 MB | 交付物所在；**本地唯一未提交改动** = `common/speculative.cpp`（B3 selector 向量化），已存成 `1cat-vllm-v100-study/wip-local-uncommitted.patch` |
| `C:\Users\<你>\.ssh\`（`id_rsa` + `known_hosts`） | 小 | 服务器免密登录；没有它就用 `QWEN.md` 里的密码 |

### 建议带（做 FA 内核要用）
| 路径 | 大小 | 说明 |
|---|---|---|
| `v100-refs/jusko-llama-volta-qwen3flash/ggml/src/ggml-cuda/`（只需该目录） | ~10 MB | 尤其 `fattn-q8-volta.cuh`（smem 内 q8_0 反量化 → `mma.m8n8k4`） |
| `v100-refs/ninfer-v100/src/ops/linear/w8/` + `ops/common/volta_mma.cuh` | ~1 MB | W8G32 ≡ Q8_0 的融合反量化 GEMM + Volta fragment 布局 |
| `.dsh/skills/` | <1 MB | 项目技能（llama-stress-test 等） |

### 不用带（可重建/只在服务器上用）
`1cat-vllm/`（113 MB）、`vllm/`（114 MB）、`vllm-forkpoint/`（86 MB）—— 只读对照，能重新 clone 或从服务器取；
`v100-refs/` 其余部分（sglang/flash-attention-v100 等，~640 MB）按需；
`.dsh/` 的会话/缓存（176 MB）不必带。

> **服务器上的东西不用复制**（换的是你的**本地设备**，不是服务器）：
> `/mnt/3.84t/v100-opt/kernel-lab/` 已存全部今天的脚本、日志、探针输出与 `ops-ub2048.txt`（导出的真实形状）。

---

## 3. 环境与路径（新设备第一件事：验证能连上）

```bash
ssh -o BatchMode=yes root@192.168.50.235 'hostname; nvidia-smi --query-gpu=index,memory.used --format=csv,noheader; systemctl is-active vllm-1cat llmscope new-api'
```
- 真开发树：`/root/llm/test/v100-opt/llama.cpp`（构建：`build-instr`；二进制 `build-instr/bin/`）
- 基线库目录：`/root/libdir-instr`（`libggml-cuda 0fb36620…`、`libggml-base a8de7c48…`）
- harness：`/root/p60-ab-harness.sh`，**必须显式** `L=/root/libdir-instr`
- 实验/脚本目录（本地盘）：`/mnt/3.84t/v100-opt/kernel-lab/`
- 模型：`/mnt/3.84t/Qwen3.8-27B-GGUF/Qwen3.8-27B-Q8_0.gguf`；draft 同理（**永远不用 `/root/llm/models/`**）
- 服务器本地树的源码 md5（今天复位后）：`mmvq.cu 19c984c9…`、`vecdotq.cuh 275e009d…`、`ggml-backend.cpp 967af7b5…`

---

## 4. ★ 今天的四个数字（引用时直接用）

1. **预填充逐算子表**（单卡、`-ub 2048`、`test-backend-ops perf --test-file`，89 个算子合计 **470.3 ms**）：
   `FLASH_ATTN_EXT`(D=256/24头/n_q=2048/**n_kv=262144**) = **368,757 µs（78.4%）**；
   `MUL_MAT result_output` = 66,518 µs（14.1%）；**除 LM head 外全部 MUL_MAT 合计 4.3%**。
2. **该 FA 的效率**：550 GFLOP / 0.369 s = **1.49 TFLOPS** = FP16 峰值(125) 的 **1.2%**；折算 16 层 ≈ 357 s，
   与实测 256K 预填充 **672 s**（370 t/s、TTFT 672 s）同量级 ⇒ **370 t/s 的主因**。
3. **解码侧 n=8 的真相**（ncu，`q8_0 m=4096 k=14336`）：n=1 与 n=8 的 **DRAM 都是 63 MB**（不是带宽问题），
   warp 指令 **7.44M → 26.16M（3.5x）** = **每条 dp4a 付 7.2 条线程指令**；L1 扇出 17x。
4. **KV 类型 A/B**（同源同库、`-ub 2048`、`-r 3`）：pp131072 **q8_0 1271.21 vs f16 879.36 t/s（f16 慢 31%）**；
   pp8192/pp32768 两者相同（+0.8%/+1.0%）⇒ 长上下文是注意力读 KV 的字节数在说话，**不是反量化**。
   ⚠️ 口径：账本旧值 pp32768=2162.44 是 `llama-bench` 默认 **f16 KV**；q8_0 KV 实测 **1920.27**。

---

## 5. 今天被否证的（**别重做**，图上都是灰节点）

| 试过什么 | 结果 | 机制 |
|---|---|---|
| Volta MMA 内核（`mma.m8n8k4` + 魔数解码，已实现且数值正确） | 238 µs vs 现役 **111 µs** | **ptxas 实测 sm_70 无 int8 mma**（`m8n8k4.s8` 非法；`m16n8k16` 要 sm_80）⇒ 走 mma 必付 ~2 指令/值的 int8→f16 解码，而 **dp4a 零解码**。**仅适用于解码（n≤8）** |
| MMVQ VDR 2→8（一线程吃完整 32 值块） | n=8 **112.40 → 131.86 µs（慢 17%）** | 少了 scale 指令，但每线程多 16 个活寄存器 ⇒ 压力换掉收益 |
| sched 快路径重试 `GGML_SCHED_RETRY_ALLOC` | 四臂同源，`bic=563 retry=563 **ok=0**` | 单槽 gallocr 的 `needs_realloc` 每轮必真（图在不同节点数之间交替）⇒ 慢路径必需 |
| `rows_per_block` 2→4/8/16（Volta ncols 5..8） | 算子级零变化；首轮三臂还因 §6 陷阱作废 | y 读取次数 =(row,token) 对数，rpb 变大 CTA 数同比变小 ⇒ 总流量不变 |
| "整条 KV 每 ubatch 反量化"是预填充主因 | 代码为真（`fattn-common.cuh:1026-1088`），但 f16 KV（零转换）131K **慢 31%** | 见 §4.4 |

---

## 6. ★★ 陷阱（今天踩到，代价是 4 个作废的臂）

1. **`/mnt/3.84t/v100-opt/llama.cpp` 是陈旧副本**（`mmvq.cu` 75866 B / `c51dc2a1…`）；它的 `build-instr/CMakeCache.txt` 里
   `CMAKE_HOME_DIRECTORY=/root/llm/test/v100-opt/llama.cpp` ⇒ **给它打补丁不生效，编译的是 /root 的源码**。
2. **harness 默认 `L=/mnt/3.84t/v100-opt/libdirs/libdir-instr` 是过期库**（`64317717…`/`d283439c…`）⇒ **不显式 `L=` 的臂全部作废**。
3. **`ggml-backend.cpp` 判状态只用 md5 + 二进制标记串**：`strings <lib> | grep -c <标记串>`。
   今天复位后重建的 `libggml-cuda.so = ad317f8b…`（≠ R257 的 `0fb36620…，源码与算子级行为一致，见账本 R265）⇒
   **引用"基线库 md5"必须写清是哪一次构建**。
4. **`GGML_META_FULLGRAPH` 只判"变量是否存在"**（`=0` 也是开）；它的口径是 B2/B3 的前置，A/B 时控制臂必须**完全不设**。

---

## 7. 明天（下一轮）的建议顺序

1. **FA-D256 诊断**（零改码，10 分钟）：`GGML_CUDA_FA_KERNEL_DEBUG=1` + 32K/128K 预填充，看 `[FAK]` 选中的内核与 D256 配置。
2. 读 `fattn.cu` 的 `ggml_cuda_get_best_fattn_kernel` + D=256 的 tile/split 参数；**先证伪"split-KV/combine 是主因"**。
3. 再决定：调 tile 常量（JS3，⚠️ X16：照抄 jusko 常量在 pp32768 是 −1.19%，**必须按形状验**）vs 移植 JS4 2-CTA vs 走 JS2 的"核内 q8_0 反量化"。
4. 任何改动都按账本纪律：同源 A/B ≥2 臂、门值一致、**tg 与 ms/轮 并列**、算子级先验（`test-backend-ops perf` 20 秒出数）。

> 另：预填充的另一块现货是 **`-ub`**（R222/R239：pp32768 1691.81(ub512) → 2162.44(ub2048) = **+27.8%**，代价是解码 −2.2%）；
> 生产 `llama-server.service` 没有 `-ub`（只读红线，需用户自己加）。
