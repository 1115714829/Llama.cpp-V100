# AUDIT-2026-09-20-dsh.md — 接手审计：哪些结论可信、哪些是弯路

> 执行体从 Qwen Code 迁到 **DSH** 后做的**只读审计**（本地档案 + 服务器实况 + 代码行级对质 + 原始日志复核 + 互联网核实）。
> 目的：确保前任留下的结论**不会把后手带回弯路**。本文件是"可信度索引"，冲突时以本文件的更正为准（红线除外）。
> 审计人：DSH（deepseek-flash）。审计对象：`QWEN.md`、`HANDOFF.md`、`goal-phase1-findings.md`、`FINAL-REPORT.md`、`baseline.md`、`phase0-round-breakdown.md`、`l3-acceptance.md`、`c5-volta-crossover.md`、`core-changes.md`。

---

## 1. 验证通过（有证据，可放心依赖）

| 项 | 证据 |
|---|---|
| llama.cpp 树 | HEAD `79504e72b`、`git status --porcelain` 空；相对 `1af554f8f`(b11053) = 13 文件 +587/−33；`[RT]` 探针 **7 处**（llama-context 4 / server-context 2 / sampling 1） |
| **三个参考仓库零改动** | `vllm@751f6807d9`、`1cat-vllm@b711d53045`（= **v1.5.0-672**）、`vllm-forkpoint@4ff865c38e`，`git status --porcelain` 全空 |
| **`/root/llm/systemd` 原始性** | 仅 7 个文件（Sep 7–17）；`vllm-1cat`/`llmscope` 为软链；生产 `llama-server.service` 与 `/etc` 副本 `diff` **逐字节相同**；项目自建 4 单元在 `/etc` 且全 disabled |
| 库 md5 | pristine `3baf1fbc` / c4 `0276aed7` / volta2 `9902c7c3` / rt `ed1b2f7f` / **nccl `faf9cc46`** —— 与档案逐字一致 |
| NCCL 确实编入 | `ldd /root/libdir-nccl/libggml-cuda.so` → `libnccl.so.2` = 1cat venv 路径 |
| 模型/语料 | Q8_0 **29,047,086,048**；draft **1,143,006,816**；Q2_K_XL 9,828,981,664；生产 Q4_K_M 18,498,573,856；`ppl-corpus.txt` md5 `6737ebfc…` |
| 起点基线 | `/root/goal-baseline.txt`：layer 70.09 / **tensor 55.95** |
| 代码行级断言 | `mmq.cuh:8` `MMQ_DP4A_MAX_BATCH_SIZE 64`（注释即 V100 那条）；`mmq.cu:334` `!fp16_mma_hardware_available(cc) \|\| ne11 < 64`；`mmq.cuh:190` Turing 门（V100 落 dp4a）；`mmq.cuh:252,279` **Volta→Ampere 配置表**；`mmvq.cuh:7`+`mmvq.cu:374-383`；`ggml-cuda.cu:391` P2P；`:1019` NCCL 门限（3 卡 `ne<131072` FP32 否则 BF16）；`allreduce.cu:402` 仅 2 卡；`:4408` graph 门限（V100 **允许**）；`fattn.cu:147` Turing 门控、`:644` 有 volta 分支 |
| C5 | 交叉点=4、ne11=4 控制组 −0.07%、生产 Q4_K_M ne11=8 **+15.0%**、nsys 见 `mul_mat_q`×3 |
| 最好成绩可溯源 | `/tmp/p60-ab-nccl-tp3{,-server}.log` 在位（94.18 t/s、AL 5.55），且该次**带 `P2P=1`** |
| 现有量具可用 | `LLAMA_ROUND_TIMING` / `LLAMA_SPEC_TIMING` 均在；`[RT] perf` 归属 `ctx_tgt`（`server-context.cpp:656 → llama_perf_context(ctx_tgt)`）⇒ harness `tail -1` 安全 |

## 2. 必须纠正的错误（★ = 会直接把人带回弯路）

| # | 错误 | 位置 | 危害 | 已处理 |
|---|---|---|---|---|
| **E1 ★** | **字节口径错**：称 1cat 为 4-bit ≈3.4 GB/卡、"我们少读 2.9×"。实测该服务是 **FP8**：target **30,866,866,928 B** + draft **3,848,817,896 B** = 34.72 GB/4 卡 = **8.68 GB/卡**；我们 30.19 GB/3 卡 = **10.06 GB/卡** ⇒ **只差 1.16×**（4 卡时我们 7.55 GB/卡**反超**） | `goal-phase1:157-159,197,523`、`HANDOFF:239` | **战略级**：会让人以为字节注定输 2.9× 而放弃 | ✅ 已更正 |
| **E2 ★** | **"每轮 ~21 ms 未归因"过期**：出自 NCCL 之前的 73 ms/轮 | `HANDOFF:17,430,437`、`goal-phase1:89-90` | 高：去追一个已不存在的量 | ✅ 已更正 |
| **E3 ★** | **量具语义错**：`llama-context.cpp:1436` 第二参数是 `batched`（非同步开关）；同步在 `:1376`、条件 `pipeline_parallel`，而 harness 永远传 `--tensor-split` ⇒ **恒不同步** ⇒ `enqueue_us` 是**异步提交窗口** | `HANDOFF:316`、`phase0:60`、`goal-phase1:100-102`、`QWEN:464`、`AGENTS.md §4.8` | 高：target 成本类结论的语义基础 | ✅ 已更正 |
| **E4** | C4 数字三套混用（36.23→37.4 / 36.32→37.35 / 35.58→36.49） | 四处 | 中 | ⚠️ 已标注（建议统一表述为"同源 A/B 三次 +2.6~2.8%"） |
| **E5** | HMMA 1.36× 出自**合成形状**（m=4096,k=14336），非模型形状；"55.95×1.36≈76" 把单算子天花板当整轮天花板 | `HANDOFF §6.3`、`goal-phase1 §18.2` | 中（方向对、理由不能用） | ✅ 已在 §18.2 更正 |
| **E6 ★** | `QWEN.md §0.3` 第 0 条与 §0.12-E 仍把**已撤回**的 HMMA 列为第一优先；`goal-phase1 §16` 与 §18 自相矛盾 | `QWEN:27-30,299`、`goal-phase1 §16` | **高：最易带回旧弯路** | ✅ 已撤回标注 |
| **E7** | c5 §4/§6 待办过期（§3.5/§3.6 已完成） | `c5:108,118-120` | 中（重复劳动） | ✅ 已标 [x] |
| **E8** | "M=8 每行理想 4.0 ms"推导错误（把 M=1 **整轮**当每行带宽下限） | `phase0:85,107` | 中 | ✅ 已更正 |
| **E9/E10** | `l3-acceptance §4`、`FINAL-REPORT §7` 的"主因是 draft 质量"已被推翻且无标记 | 两份文档 | 中 | ✅ 已加取代抬头 |
| **E11 ★** | **"GDN+attention 只占 3–6%"基于形状不匹配的微基准**（hsk=256/512/64、nh=1/2） | `HANDOFF §10` | 高（对长上下文） | ✅ 已标注 + 列入 Phase B2 重测 |
| **E12** | `llama-server-l3-{c4,pristine}.service` 未记录 | — | 低 | ✅ 已记入 AGENTS.md §2 |
| **E13** | `HANDOFF §12` 复现命令**漏 `P2P=1`** | `HANDOFF:464` | 中 | ✅ 已补 |
| **E14 ★** | **"TP3 最优 / 跨岛更慢"的选卡引导** | `QWEN §0.6`、`HANDOFF §2/§5`、`AGENTS.md §3` | 高 | ✅ 已按用户指示改为"卡数 1–6 自由、跨岛非瓶颈" |
| **E15 ★** | **模型几何写错**：`head_dim=128 / n_head=40 / n_kv_head=8`；`core-changes §8` 又"反推"成 40/4 —— **两套都错** | `baseline.md`、`core-changes §1/§4`、`goal-phase1 §6` | **高**：所有 FA 形状判断失效 | ✅ 已按 GGUF 实测更正 |
| **E16 ★** | **prefill/TTFT 被整体低估**：我们 256K prefill **370 t/s / TTFT 672 s**（占交互 99%）；1cat 公开 32K/64K prefill **3,567–4,069 t/s** | `l3-acceptance`、`HANDOFF §10` | 高（用户真实场景） | ✅ 已升为第一优先 |

## 3. 修正后的关键事实

### F1 当前最好构建的每轮账本（NCCL TP3 + P2P，Q8_0 + DFlash2 n=7，**58.9 ms/轮** = AL 5.55 ÷ 94.18 t/s）

| 成分 | ms/轮 | 占比 |
|---|---:|---:|
| target 整步（`[RT] target decode+sync`） | **32.3–34.3** | 55–58% |
| ↳ 其中 `enqueue`（**异步提交窗口**，非 GPU 时间） | 21.8 | 37% |
| ↳ 其中主机侧图工作（build 0.13 + alloc 2.09 + setin 0.04） | 2.3 | 4% |
| ↳ 其余（同步等待/尾部） | ~9 | 15% |
| draft 前向（`spec timing: draft_decode`） | **13.8–14.4** | 24% |
| CPU selector | 4.3 | 7% |
| walk | 0.1 | 0% |
| **未归因余量** | **~6–8** | 11% |

### F8 ★★ 跨设备（allreduce）路径 = 每轮最大单项（2026-09-20 从档案原始日志中挖出，零成本）

**同 build、同模型、同 harness，唯一变量是「有没有跨设备集合通信」**（`--split-mode layer` 无 allreduce vs `tensor` 有）：

| 配置 | 卡数 | allreduce | rounds | enqueue_us（累计） | **ms/轮** | alloc ms/轮 |
|---|---:|---|---:|---:|---:|---:|
| `p60-ab-bf-layer` | 3 | **无** | 278 | 1,264,602 | **4.55** | 0.15 |
| `p60-final-nccl-tp3`（= **95.20 那次**） | 3 | NCCL | 294 | 6,310,375 | **21.46** | 2.15 |
| `p60-ab-bf-tp3`（graph-on，P2P） | 3 | butterfly | 302 | 7,397,386 | **24.49** | 2.27 |
| `p60-ab-nccl-tp4` | 4 | NCCL | 334 | 8,226,662 | **24.63** | 2.17 |
| `p60-ab-nccl-tp6` | 6 | NCCL | 303 | 11,826,596 | **39.03** | 2.59 |

⇒ **tensor 与 layer 在 3 卡上差 16.9 ms/轮（21.46 − 4.55）**，这就是纯跨设备开销；
⇒ 换成 NCCL 只把 butterfly 的 24.49 压到 21.46 ⇒ **瓶颈是「每次集合通信的固定延迟」，不是带宽、也不是传输实现**；
⇒ 随卡数恶化（3→4→6：21.5→24.6→39.0），与档案里 `[RT] target decode+sync` 的 32.4→48.6→73.0 同向。

**对照 1cat**：他们 TP4 的 push all-reduce **整桶只有 1.294 ms/轮**（`sm70_dflash2_target_graph_20ms.md:758-761`，子代理取证）⇒ **我们在这条路上差约 13×**。
这条**比 draft（~10 ms）与 selector（4.3 ms）都大**，且与「已排除的 NCCL 调参」不矛盾：调参解决不了每次集合的固定延迟，**要换协议**（1cat 的 one-shot push-broadcast + 固定 rank 序归约，80 KiB 时 6.64 µs/次）。

**未定的一个数**：每轮到底几次集合通信（用于把 17 ms 摊到单次）。子代理给的估算路径：若是 ~128 次/轮（2 次/层 × 64 层）× 160 KiB，则单次 ≈ 133 µs。**B1/B2 量具会给出独立证据**。

### F2 与 1cat 的差距是**效率**，不是字节
- 字节：我们 **10.06 GB/卡/轮**（3 卡）vs 1cat **8.68 GB/卡/轮**（FP8，4 卡）⇒ **1.16×**；**4 卡时我们 7.55 GB/卡，反超**。
- 时间：**58.9 ms/轮 vs 17.463 ms/轮 = 3.37×**。
- 每卡有效带宽：我们 ~171 GB/s vs 他们 ~497 GB/s。

### F3 ★ draft 前向 = **一轮一次块前向**，且被 overhead 主导（2026-09-20 B3 实测，终结 HANDOFF 待办 2a）

**实验**：固定一切（Q8_0 + DFlash2、3 卡 0/1/2 tensor、`libdir-nccl` + `P2P=1`、NODROP 诊断），只把 `--spec-draft-n-max` 从 1 → 3 → 7 变化：

| n_max | `draft_decode` ms/轮 | selector ms/轮 |
|---:|---:|---:|
| 1 | **12.59** | 3.61 |
| 3 | **12.75** | 3.82 |
| 7 | **13.99** | 4.50 |

⇒ **几乎不随 n_max 变化**（1→7 只 +1.4 ms / +11%）⇒ **draft 每轮只做一次"块前向"**（DFlash2 的并行块草拟），**不是每 token 一次**。
⇒ 因此 draft 的 12.6–14 ms/轮**不是带宽**：1.14 GB / 13 ms ≈ **88 GB/s**；若带宽受限（读 1.14 GB @ ~700 GB/s）应约 **1.6 ms** ⇒ **头寸 ~8×**（HANDOFF 里"8-12× vs 1.5×"的悬案：**8× 那一侧成立**）。
⇒ 与 layer 模式对照（`draft_decode` layer **4.44 ms** vs tensor **13.25 ms**）合并解读：**约 9 ms/轮 是 draft 在 tensor 下白付的 allreduce**，其余 ~4 ms 是发射/同步。
⇒ **结论：P2（draft 侧编排）是本项目最确定的一块"自己的钱"** —— 目标把它从 13–14 ms 压到 4–5 ms（先摘掉 allreduce，再谈入图）。

### F3b draft 在 tensor 下白付 allreduce（原始记录）
`draft_decode`：tensor **13.25–14.4 ms/轮** vs layer **4.44 ms/轮** ⇒ **~9 ms/轮（约 15%）**。
根因已定但未解决：`src/models/dflash.cpp:172-175` —— 我们的 DFlash2 draft 是**全词表 draft、借用 target 的 lm head**（GGUF 无 `output.weight`）⇒ `--spec-draft-device` 必崩（`Meta()` 占位缓冲）。

### F4 接受长度不是差距来源
- 1cat v1.5.0 公开：GSM8K 64-prompt **AL request-mean 5.3888 / pooled 4.5644**（官方 5.46）。
- **DFlash2 官方模型卡**（z-lab，目标 `Q4_K_M`、temp 1.0/top_p 0.95/top_k 20、xhigh、GSM8K 前 8 题）：**Q4_K_M draft = AL 5.39**（BF16 5.28 / Q8_0 5.13）。
- 我们实测 **4.11–6.08**（NCCL 那次 5.55）⇒ **与官方同级**。

### F5 1cat 用 TP4 是硬约束逼出来的
`config.json`：`head_dim 256`、`num_attention_heads 24`、`num_key_value_heads 4` ⇒ vLLM 要求 TP 整除头数 ⇒ TP∈{1,2,4}；FP8 34.72 GB 在 16 GB 卡上 TP2 放不下 ⇒ **只剩 TP4**。
⇒ 对我们**不构成约束**（llama.cpp 允许任意切分）；且 1cat 的 TP4 本身就是**跨岛**（0,1,3,4）。

### F6 长上下文 = attention 的世界（用户真实场景）
- 本模型：**`head_dim=256` / 24 Q 头 / 4 KV 头（GQA=6）/ 16 层全注意力 + 48 层 GDN**（GGUF 实测，见 §4）。
- 我们：256K prefill **370 t/s**（TTFT **672 s**，占 256K 交互耗时 ~99%）；256K decode（关 MTP）**33.97 t/s**。
- 1cat 公开：**32K cold prefill 4,039–4,069 t/s、64K pure prefill 3,567 t/s**；attention 有用算力 **17.92 → 47.1 → 60.8 TFLOP/s**（v1.3.0 的 D256 Split-D/N32 → PR#286 GQA-packed wide QK/PV）。

### F7 模型几何（GGUF 硬证据，勿再写错）
用 1cat venv 的 `gguf` 包直读我们实际加载的 `Qwen3.8-27B-Q8_0.gguf`：
```
qwen35.attention.head_count    = 24
qwen35.attention.head_count_kv = 4
qwen35.attention.key_length    = 256
qwen35.attention.value_length  = 256
qwen35.embedding_length        = 5120
qwen35.block_count             = 65      (64 主体 + 1 MTP)
qwen35.full_attention_interval = 4
qwen35.ssm = { conv_kernel 4, group_count 16, inner_size 6144, state_size 128, time_step_rank 48 }
```
⇒ 与 1cat 侧 `config.json`（24 / 4 / head_dim 256）一致；**档案里的 128/40/8 与 40/4 都是错的**。

## 4. 本次已落地的文档更正（不改历史，只加更正块）

| 文件 | 改动 |
|---|---|
| `QWEN.md` | §0.3 顶部加审计更正块 + 第 0 条撤回；§0.12-E 加撤回标注；§0.6 拓扑段改为"卡数 1–6 自由、跨岛非瓶颈"；§0.6 操作红线改为 **`/root/llm/systemd/` 整目录只读** |
| `HANDOFF.md` | §6.2 加"旧表=NCCL 之前"标注 + **新账本表** + E3 量具更正；§7.1 字节数更正 + TP4 硬约束解释；§2 拓扑与 §5 卡数表加"历史实测"降级；§10 待办 2b 改口径；§12 复现命令补 `P2P=1` 与 4 卡示例 |
| `goal-phase1-findings.md` | §8 字节口径整段更正；§4 加 NCCL 前标注与新账本；§16 卡数与"唯一杠杆"两行更正并交叉引用 §18；§18.2 更正字节与接受率 |
| `phase0-round-breakdown.md` | §4.2 推导更正（2.9× 的正确口径）+ roadmap 行撤回 |
| `core-changes.md` | §4 模型几何更正（D=256 / 24Q / 4KV）并说明连带失效 |
| `baseline.md` | 模型几何更正 + GGUF 硬证据 |
| `l3-acceptance.md` / `FINAL-REPORT.md` | §4 / §7 加"结论已被取代"抬头 |
| `c5-volta-crossover.md` | §6 两条待办标 [x] |
| `AGENTS.md`（工作区根） | 红线加 systemd 目录；卡数自由；度量纪律第 8/9/12 条（E3、P2P 复现、模型几何）；新账本；真正待查重排 |
| 新增 | 本文件、`1CAT-PORT-BACKLOG.md`、`EXTERNAL-REFS.md`、`CODEX-ARCHIVE-INDEX.md` |

> 说明：以上改动**均未 commit**（红线：不代提交）。study 仓库的改动留给用户决定。

## 5. 仍然存疑 / 需要实测才能定论的（不要当结论用）

1. **长上下文 FA 的真实占比**：现有 opbench 形状不匹配（E11）⇒ 必须用 Phase B2 的 per-op 计时在真实图里量。
2. **`enqueue_us` 的物理含义**：它是异步提交窗口，但其中多少是 host launch、多少是跨设备拷贝等待，未分离。
3. **draft 前向一轮几次**（HANDOFF 待办 2a）：一轮一次 ⇒ ~83 GB/s（头寸 8–12×）；一轮 8 次 ⇒ ~660 GB/s（头寸 1.5×）。
4. **卡数重扫**：早期 TP3 结论在 butterfly 时代 + 短上下文标尺下取得，且用户已明确跨岛非瓶颈。
5. **1cat 的 17.463 ms/轮 vs v1.5.0 的 17.38 ms/轮**：后者是"无需把整服务配成 max_num_seqs=1"的路线，两者契约略有差别。
