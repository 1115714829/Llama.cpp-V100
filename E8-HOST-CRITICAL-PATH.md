# E8 判决：主机时间确实在关键路径上 —— 且给出了可标定的杠杆（R191）

## 实验（同源四臂，env GGML_META_HOST_SPIN_US，端口 8271-8274，NPRED=512 NODROP）
在 ggml_backend_meta_graph_compute 末尾注入已知忙等 3 ms/call（默认 0 = 今日逐位相同）：

| 臂 | p1 pred_ms | p2 pred_ms | p3 pred_ms | MEDIAN_TG |
|---|---:|---:|---:|---:|
| spin0a (0) | 5262.502 | 6486.297 | 2718.765 | 97.10 |
| spin0b (0) | 5262.553 | 6488.441 | 2716.602 | 97.10 |
| spin2a (3000) | 5762.777 | 7143.876 | 3005.097 | 88.67 |
| spin2b (3000) | 5747.978 | 7147.924 | 2980.420 | 88.90 |

四臂 sha256 = f3edac19446ef641447f8391c71cb1b25a74055d662e9887890fa4a98602ca34（全一致）；
**对照两臂差 0.001%**（5262.502 / 5262.553）=> 本测量精度极高，5% 级的效应不可能被噪声解释。

## 折算（每轮）
| prompt | ms/轮 对照 | ms/轮 spin | 差 | tg 变化 |
|---|---:|---:|---:|---|
| p1 (AL 5.55) | 57.38 | 62.84 / 62.68 | **+5.4** | 97.10 -> 88.7/88.9 (-8.7%) |
| p2 (AL 4.22) | 53.85 | 59.28 / 59.32 | **+5.5** | 78.78 -> 71.5 (-9.2%) |
| p3 (AL 6.38) | 56.64 | 62.61 / 62.09 | **+5.7** | 112.18 -> 101.5/102.3 (-8.9%) |

注入量 = 3 ms/call x ~3.3 call/轮 = **9.9 ms/轮**；实测只体现 **5.4-5.7 ms/轮** =>
**穿透率约 55%**（其余 45% 与 GPU 执行重叠）。

### ⇒ 标定杠杆（本会话第二个可用的定量结论）
**每 1 ms/call 的主机时间 ≈ 1.8 ms/轮**（在 AL 5.55 附近；p2/p3 同量级）。
反过来：当前 [META] total 10.66 ms/call x 3.3 call/轮 = **35.2 ms/轮**的主机时间，其中约 **19.2 ms/轮 落在关键路径上**。

## 结论与它对前面结论的修正
1. **主机时间确实在关键路径上（~55% 穿透）** => 主机侧优化**有效**，与 E5 的方向一致；
2. 但 **E7 仍然成立**：direct/capture 只占主机时间的一小部分（删掉 3539 次 direct 约 1 s/臂，摊到每轮仅 ~4 ms 的 1/10 量级），
   所以"删 direct 没收益"与"主机时间值 1.8x"**并不矛盾** —— 关键在**这个主机时间具体是什么**；
3. ⇒ 要拿到 -19.8 ms/轮（56.8 -> 37.0），需要砍掉约 **11 ms/call** 的主机时间 —— 而当前 total 只有 10.66 ms/call。
   也就是说：**必须几乎砍光主机时间才够**，所以下一步不是"优化某一项"，而是**先把 dev = 7.19 ms/call（占 67%）拆开**；
4. 已知的三块：**dev 7.19**（149 compute/call，48 us each，成分不明）/ **ar 2.22**（46.5 次 AR，47.7 us each = NCCL 启动）/ **prologue 1.25**（重建）。
   - `ar` 与 `prologue` 都有**明确的外部先例**：AR 可用图内 AR（E1 push AR / device-side）省掉 46 次主机启动；prologue 是 N6a/N6b 的靶子；
   - `dev` 是最大且最不明的一块，**必须先拆**（例如按 subgraph 索引/设备分别计时，或统计每条 compute 的分布）。

## 现场
- 补丁 `GGML_META_HOST_SPIN_US`（+13 行，默认 0 = 逐位相同）**已上机验证**；WIP 存档 `wip-host-spin.patch`；
- 四臂 STATUS=OK、无 LAUNCH_FAILED；服务 inactive/active/active 未变。