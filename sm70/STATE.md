# STATE · 当前真相（主代理维护，≤ 1 页）

更新：2026-09-26 13:30 · 阶段：**0（基线）** · 下一步：用户裁定包络与 TPOT 降噪口径 → S0-07（剖析定序）

## 已定决策

- **D1 测试路线**：日常以单卡为主（L1 算子级 + L2 单卡端到端，IQ1_S、GPU 2）迭代内核内部流程；L3（256K、4 卡）只做基线、里程碑和验收（LEDGER L0002、L0004）。
- **D2 派发**：主代理用 `tools/dsh-run.ps1` 直接驱动 DSH headless（模型 mimo-v2.6-pro，权限 workspace-write）。
- **D3 REF 重测**：用户已授权 S0-05 由执行者启停 `vllm-1cat`，测完恢复 inactive（LEDGER L0003）。
- **D4 BASE 配置 = 代码默认**（不设任何 env）：旧三开关预填充快 7.4%，但卡 4 显存 14,990 MiB 超包络且单卡 OOM ⇒ 列为 C3（LEDGER L0010）。

## 基线

L3（256K、prompt 236,313 token、卡 0,1,3,4；S0-04）：

| 指标 | REF（1cat-vLLM，S0-05） | BASE（代码默认） | 差距 | 参考：旧三开关 | 参考：BASE 关投机 |
|---|---|---|---|---|---|
| TTFT 中位 | **151.42 s**（SPREAD 0.2%） | **212.69 s**（SPREAD 0.7%） | **1.40×** | 196.92 s | 174.83 s |
| pp t/s | 1,561 | 1,111 | | 1,200 | 1,352 |
| TPOT 中位（3 rep） | **9.13 ms**（8.76 / 9.13 / 10.27） | **20.41 ms**（18.65 / 20.41 / 29.62） | **2.24×** | 27.69 ms | 33.39 ms（SPREAD 0.9%） |
| AL / 每轮 ms（3 rep） | —（草稿 7 token/块） | 3.17–5.64 / 88.1–103.8 | | 3.26–4.67 / 89.2–93.3 | — |
| 每卡显存峰值 MiB | **15,042–15,106** | ≤ 13,806 | 更省 | 14,732–14,990 | ≤ 13,014 |

- 噪声带：TTFT 1.6%；关投机 TPOT 1.8%；投机 TPOT 31–54% 离散 ⇒ 不能直接作 A/B 判据（C2）。
- 关投机与开投机的 TTFT 差 ≈ 22–38 s = 草稿模型的预填充成本。

L2（单卡 IQ1_S，64K、prompt 55,286 token，GPU 2；只作相对比较；S0-03 臂 B）：TTFT 68.09 s（pp 812，SPREAD 1.4%）；每轮 101.5 / 93.6 / 97.0 ms；TPOT 81.7 / 38.0 / 25.5 ms（AL 1.50 / 2.43 / 3.85）；显存 14,606 MiB。

## BASE 源码与二进制（S0-02）

- 源码：`llama.cpp` `5cefb8904`（tag `sm70-base0`）；二进制：`/mnt/3.84t/sm70/libs/base0/`（`libggml-cuda` md5 `d28f8810…`）。
- 256K prompt：`/root/llm/test/sm70/prompt-256k.txt`（sha256 `bd7038a0…`）；L2 prompt：`prompt-l2.txt`（前 270,000 字节）。
- L2 门参考（S0-06）：`/root/llm/test/sm70/gate/REF-1card.txt`、`REF-4card.txt`（BASE 重启前后逐 token 一致）。

## 工作队列（S0-07 后正式排序）

候选（阶段 0 中发现，未排序）：
- **C1** `LLAMA_SM70_79T` 预填充引擎在单卡形状（4 KV 头）下 `fattn-sm70-d256.cu:935` 显存申请失败即 abort：应按剩余显存分块 / 回退（S0-03）。
- **C2** 投机 TPOT 随接受率剧烈漂移（L2 25–82 ms、L3 18.7–29.6 ms）：验收口径需要降噪方案，待 S0-05 数据后提给用户裁定（改 BENCH 须用户同意）。
- **C3** 旧三开关（79T 预填充引擎等）TTFT −7.4%，但 4 卡显存超包络 163 MiB：先把显存压进包络（并修 C1），再按流程评估采纳（S0-04）。

## 待用户决定

（无；C2 待 S0-05 后提出）
