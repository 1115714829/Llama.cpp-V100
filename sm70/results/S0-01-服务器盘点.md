# S0-01 结果 · 服务器盘点

- **执行者**：主代理（只读；工具 `tools/srv-status.sh`、`tools/srv-facts.sh`）
- **时间**：2026-09-26 11:00–11:10
- **状态**：完成

## 1. 机器

| 项 | 值 |
|---|---|
| GPU | 6× Tesla V100-SXM2-16GB；GPU0–3 0 MiB，GPU4 142 MiB，GPU5 396 MiB（无计算进程）；利用率全 0% |
| 时钟 / 功耗 | 6 卡同为：persistence 开、应用时钟 1312 MHz、最大 1530 MHz、功耗上限 300 W |
| 拓扑 | GPU0/1/2 = NUMA0，GPU3/4/5 = NUMA8；组内 NV2，跨组 SYS；GPU 显存以 NUMA 节点 250–255 暴露 |
| 内存 / 磁盘 | 153 GB 内存（可用 145 GB）；`/`（NFS）剩 38 GB；`/mnt/3.84t`（本地 NVMe）剩 2.5 TB |
| CUDA | 12.4.131 |

## 2. 服务与占用

`vllm-1cat` inactive · `llmscope` active · `new-api` active · `llama-server`（生产单元）inactive · `1cat-gpu-init` active（只做 modprobe，不改时钟）。无 tmux 会话、无相关进程、无锁文件。

## 3. 模型

| 用途 | 路径 | 大小 |
|---|---|---|
| llama 主模型 | `/mnt/3.84t/Qwen3.8-27B-GGUF/Qwen3.8-27B-Q8_0.gguf` | 29,047,086,048 B |
| 单卡调测 | `/mnt/3.84t/Qwen3.8-27B-GGUF/Qwen3.8-27B-UD-IQ1_S.gguf` | 6,192,222,208 B |
| llama 草稿 | `/mnt/3.84t/llm-models/Qwen3.8-27B-DFlash2-GGUF/Qwen3.8-27B-DFlash2-F16.gguf`（另有 Q8_0、Q4_K_M） | 3,860,292,608 B |
| vLLM 主模型 | `/root/llm/models/Qwen3.8-27B-FP8`（fp8 e4m3，动态激活） | — |
| vLLM 草稿 | `/root/llm/models/Qwen3.8-27B-DFlash2`（BF16 safetensors） | 3,848,817,896 B |

## 4. vLLM 标准单元（`/root/llm/systemd/vllm-1cat.service`，只读）

TP4、`CUDA_VISIBLE_DEVICES=0,1,3,4`、`--dtype half`、`--kv-cache-dtype fp8_e5m2`、`--gpu-memory-utilization 0.905`、`--max-model-len 262144`、`--max-num-seqs 1`、chunked prefill + `--max-num-batched-tokens 2048`、prefix caching 开、`--attention-backend FLASH_ATTN_V100`、speculative = dflash（草稿 BF16，`draft_sample_method probabilistic`）、thinking 开（reasoning_effort xhigh）、端口 8000、带 api-key。

## 5. llama.cpp 现有构建

- `/root/libdir-gb`：构建于 2026-09-26 09:52，`BUILD_RC=0`，`libggml-cuda` md5 `d28f8810ccdd9ccf7a96d4c387cfb7fc`，`llama-server` md5 `47467cf6…`；含 `test-backend-ops`、`test-export-graph-ops`、`llama-perplexity`、`llama-bench`。
- `llama-server --help` 确认对齐表所需参数全部存在：`-sm tensor`、`-b/-ub`、`-fa`、`-ctk/-ctv`、`--spec-type draft-dflash`、`--spec-draft-n-max`、`--model-draft`、`--reasoning`、`--reasoning-effort`、`--reasoning-preserve`、`--chat-template-kwargs`。
- 本地 `llama.cpp` 的 6 个未提交文件与服务器构建树逐字节一致（CR 归一化 md5 抽查）。

## 6. 对体系的影响

- L3 卡位定为 0,1,3,4（与 vLLM 相同）；单卡开发用 GPU 2（空闲且不在 L3 卡组内）。
- 草稿用 F16 GGUF，对齐 vLLM 的 BF16 / half 草稿。
- 构建产物放 `/mnt/3.84t/sm70/libs/`（`/` 只剩 38 GB）。

---
## 审核意见（主代理填写）

主代理自执行，已并入 STATE.md。
