# S0-04 结果

- **任务包**：tasks/S0-04-BASE-256K.md
- **执行者 / 模型**：DSH / mimo-v2.6-pro
- **开始 — 结束**：2026-09-26 12:29（push + lock）— 12:37（unlock 后收尾完）
- **状态**：停手上报（原因：任务包臂 A 步骤 3 的 `rjob.sh s004a` 发令即被脚本拒绝 `RJOB_REFUSED session s004a exists`（rjob.sh exit 2）——bench 作业名 `s004a` 与在跑的 srv 会话名 `s004asrv` 构成 tmux 前缀匹配撞车，3 rep 一个都没跑；臂 B/C 的命名（`s004b`/`s004bsrv`、`s004c`/`s004csrv`）是同一结构，判定必然同样被拒，按技能「脚本报错 → 停手上报」未继续发臂 B/C）

## 1. 上机记录（不上机写「未上机」）

| 项 | 值 |
|---|---|
| lock.sh 输出 | `LOCK_OK id=S0-04 cards=0,1,3,4` |
| 使用的库目录 + BUILD_MANIFEST 摘要 | `/mnt/3.84t/sm70/libs/base0`；BUILD_TIME=2026-09-26T09:52:27+08:00、TREE=/root/llm/test/v100-opt/llama.cpp、LIBS=/root/libdir-gb、MANIFEST_SHA256=e849141cfa6e0f12961b8c757222319b1223f75100ea63baac1871f44b551a0e、MANIFEST_FILES=2082、LIBGGML_CUDA_MD5=d28f8810ccdd9ccf7a96d4c387cfb7fc、LLAMA_SERVER_MD5=47467cf6f2234cf73c87cbc3fa239433、BUILD_RC=0（出处：raw/S0-04/s004asrv.log 行 3–10） |
| 使用的卡 | 0,1,3,4（`STD_LAUNCH … cards=0,1,3,4`，raw/S0-04/s004asrv.log 行 1） |
| unlock.sh 输出 | `UNLOCK_OK id=S0-04` |
| 收尾后 srv-status 摘要（服务状态、GPU 显存） | 12:34:51，load 0.38/0.43/0.44；GPU 0/1/2/3/4/5 全部 0 MiB、0%；gpu_apps 空；`vllm-1cat=inactive`、`llmscope=active`、`new-api=active`、`llama-server=inactive`；tmux `no server running`；AGENT_LOCK 不存在 |

## 2. 结果（每个数字写出处：服务器文件 + 行，或本地 raw/ 路径）

### 2.1 测量数据

**无**。三个臂的 bench 均未运行：无 summary 表、无 SPREAD、无 MEM_PEAK、无 `timings=` JSON。`runs/s004a|s004b|s004c` 三个目录在服务器上不存在（收尾 scp 报 `No such file or directory`，见 4.）。

### 2.2 原始报错（现场，臂 A 步骤 3 第 1 条命令）

```
PS> ssh -o BatchMode=yes root@192.168.50.235 'bash /root/llm/test/sm70/tools/rjob.sh s004a env TAG=s004a REPS=3 bash /root/llm/test/sm70/tools/bench-256k.sh'
RJOB_REFUSED session s004a exists
[exit code: 1]
```

（外壳 exit code 1；rjob.sh 自身 `exit 2`，见 2.3 行号。）

### 2.3 诊断证据（只读命令，未改服务器状态）

1. `tmux ls` 全文：`s004asrv: 1 windows (created Sat Sep 26 12:29:38 2026) [80x24]` —— 只有 `s004asrv` 一个会话，**不存在**名为 `s004a` 的会话。
2. `rjob.sh`（本地 sm70/tools/rjob.sh 行 15）：`if tmux has-session -t "$NAME" 2>/dev/null; then echo "RJOB_REFUSED session $NAME exists"; exit 2; fi`。tmux 的 `-t` 目标按「精确 → 前缀 → glob」顺序匹配，`-t s004a` 命中前缀更长的在跑会话 `s004asrv` ⇒ 只要 srv 会话活着，`rjob.sh s004a` 必被拒。
3. `ls -la /root/llm/test/sm70/logs/`：S0-03 留下的包装脚本是 `rjob-s003abench.sh`（12:10）与 `rjob-s003asrv.sh`（12:09）——S0-03 的 bench 作业名带 `bench` 后缀，与其 srv 会话名 `s003asrv` 无前缀关系；本任务包步骤 3/5/6 用的作业名是 `s004a`/`s004b`/`s004c`，与其 srv 会话名 `s004asrv`/`s004bsrv`/`s004csrv` 逐一构成前缀。
4. 臂 B/C 未发令，`s004b`/`s004c` 撞车为按上述命名结构的推断，非实测。

### 2.4 臂 A 服务侧记录（服务本身正常，失败点不在服务）

- `STD_LAUNCH tag=s004a libs=/mnt/3.84t/sm70/libs/base0 cards=0,1,3,4 model=/mnt/3.84t/Qwen3.8-27B-GGUF/Qwen3.8-27B-Q8_0.gguf ctx=262144 spec=1 nmax=7 port=8095 date=2026-09-26T12:29:38+08:00`（raw/S0-04/s004asrv.log 行 1）
- `STD_ENV GGML_CUDA_FA_KERNEL_DEBUG=1 GGML_GALLOCR_SLOTS=1 GGML_META_SUBGRAPH_CAPTURE=1 LLAMA_SM70_79T=1`（行 2）
- wait-health 输出：`HEALTH_OK port=8095 after=0s`
- 服务日志末尾 60 行（判定表要求，捕获于停服务前；全文在 raw/S0-04/s004asrv.log）：45 行 `[FAKD] n=16…n=60`（dev 0–3 循环，`kernel=200 q=2 kv=256 Ktype=8 hq=6 hkv=1 Knb2=272`）+ 以下 15 行原样：

```
0.15.337.892 I common_speculative_init_result: capping draft context ubatch from 2048 to 64 (block draft)
0.15.337.901 I common_speculative_init_result: loading draft model '/mnt/3.84t/llm-models/Qwen3.8-27B-DFlash2-GGUF/Qwen3.8-27B-DFlash2-F16.gguf'
0.16.112.904 W model has unused tensor selector_predecessor.weight (size = 127139840 bytes) -- ignoring
0.16.112.915 W model has unused tensor selector_successor.weight (size = 127139840 bytes) -- ignoring
[FAK] kernel=MMA_F16 D=128 n_q=64 n_kv=2304 kv_type=1 need_f16_K=1 need_f16_V=1
[FAK] kernel=TILE D=128 n_q=1 n_kv=2304 kv_type=1 need_f16_K=1 need_f16_V=1
0.17.953.669 I srv    load_model: initializing, n_slots = 1, n_ctx_slot = 262144, kv_unified = 'false'
0.18.488.038 I common_speculative_impl_draft_dflash: adding speculative implementation 'draft-dflash'
0.18.488.053 I common_speculative_impl_draft_dflash: - n_max=7, n_min=0, p_min=0.00
0.18.488.054 I common_speculative_impl_draft_dflash: - block_size=8, mask_token_id=248070, n_extract=5, sample_from_anchor=true
[RT] llama_decode entered: n_tokens=2
[RT] ctx::decode entered: n_tokens=2
[FAK] kernel=TILE D=128 n_q=2 n_kv=256 kv_type=1 need_f16_K=1 need_f16_V=1
0.18.715.988 I srv  llama_server: model loaded
0.18.716.001 I srv  llama_server: listening on http://127.0.0.1:8095
```

- 停服务与作业收尾：`STOPPED port=8095 after=10s`；`rwait.sh s004asrv 120 5` 输出 `== JOB_DONE_s004asrv after 0s ==`、`JOB_DONE_s004asrv rc=0`，尾部两行为 `4.55.061.505 I srv operator(): operator(): cleaning up before exit...` 与 `JOB_DONE_s004asrv rc=0`。
- draft/accept 行与 error 计数：任务包步骤 4 的 grep 未执行（见 4.）；停手前 `tail -60` 中无 error/abort 行（出处同上）。

## 3. 观察（只写看到的事实，不写推测）

- 臂 A 服务启动、加载、监听全部正常：`HEALTH_OK port=8095 after=0s`；主模型 Qwen3.8-27B-Q8_0 + 草稿模型 Qwen3.8-27B-DFlash2-F16，`spec=1 nmax=7`，`n_slots = 1, n_ctx_slot = 262144`。
- 唯一失败点是 bench 作业发令被 `rjob.sh` 拒绝（2.2）；bench 的 3 个 rep 一个都没运行。
- 被拒时 tmux 只有 `s004asrv` 一个会话（2.3-1）。
- 停机后 6 张卡全部 0 MiB、0%；GPU4/5 无 S0-01 记录的 142/396 MiB 非计算占用（照实记录，未做解释）。
- `vllm-1cat` 全程 inactive，未启停任何服务；锁已放（`UNLOCK_OK id=S0-04`），tmux 无残留会话。

## 4. 异常与偏离任务包之处

- **停手点**：臂 A 步骤 3 第 1 条（`rjob.sh s004a` 发令）即 `RJOB_REFUSED`。臂 B/C 的 rjob 名与其 srv 会话名是同一前缀结构（2.3-3/4），判定会同样被拒，故未发臂 B/C：技能规定「脚本报错 → 停手上报」；若按判定表把三臂逐个记失败，结论也是「三臂都失败」⇒ 停手上报。
- 未执行：臂 A 步骤 3 第 2 条 rwait（无作业可等）、步骤 4 第 1 条 grep（draft/accept、error 计数、FAK 统计——无负载运行，没有对象）。步骤 4 的 stop-llama.sh、rwait s004asrv 与整个第 7 步收尾均照抄执行。
- 只读补充 4 条：`tmux ls`、`ls -la /root/llm/test/sm70/logs/`、`tail -60 /root/llm/test/sm70/logs/s004asrv.log`（判定表要求）、`head -12 /root/llm/test/sm70/logs/s004asrv.log`（取 BUILD_MANIFEST）。均未改服务器状态。
- 收尾 scp 照抄执行的结果：runs scp 三个源目录均不存在（`No such file or directory`，exit 1，无文件拷回）；logs scp 拷回 `s004asrv.log`，`s004bsrv.log`/`s004csrv.log` 不存在报同样错误。
- 未改任何脚本与源码、未构建、未动 `vllm-1cat`；未打印密钥。

## 5. 产出文件清单

本地（`sm70/results/raw/S0-04/`）：

- `s004asrv.log`（臂 A 服务日志全文，唯一原始产物）

服务器：`/root/llm/test/sm70/logs/s004asrv.log`、`/root/llm/test/sm70/logs/rjob-s004asrv.sh`（现场保留，未删）。

---
## 审核意见（主代理填写）

**驳回并重派**（主代理，2026-09-26 12:40）。执行者处置正确（拒绝即停、停服务、解锁）。根因是主代理的工具缺陷：rjob.sh 用 `tmux has-session -t <名>`，tmux 按前缀匹配，s004a 撞上正在运行的 s004asrv。已改为精确匹配 `-t =<名>` 并在服务器上用同构用例验证。附带确认：臂 A 服务（4 卡、256K、旧三开关）18.7 s 加载完成并正常监听。
