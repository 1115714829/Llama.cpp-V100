# E7 判决：形状敏感的 CUDA 图 key —— 机制成立，性能为零（R189）

## 实验（同源 A/B，四臂，端口 8251/8252/8253/8254，NPRED=512 NODROP，env GGML_CUDA_GRAPH_KEY_SHAPE）
| 臂 | MEDIAN_TG | capture | replay | direct | sha256 |
|---|---:|---:|---:|---:|---|
| gkc0a (C0) | 97.63 | 2838 | 103977 | 17345 | f3edac19... OK |
| gkc0b (C0) | 97.16 | 2838 | 103977 | 17345 | f3edac19... OK |
| gkfa (key 形状敏感) | 97.24 | 2639 | 107715 | 13806 | f3edac19... OK |
| gkfb (key 形状敏感) | 97.38 | 2634 | 107715 | 13811 | f3edac19... OK |

## 结论
1. **机制成立（预测逐项吻合）**：把 key 从 nodes[0] 改成 (nodes[0], n_nodes, ne, op) 后，direct **17345 -> 13806/13811（-20.4%）**、
   replay 103977 -> 107715（+3.6%）、capture 2838 -> 2634/2639。这证明 E5 诊断（同一 key 被两种形状共用）是**对的**。
2. **性能为零（决定性否定）**：C0 均值 **97.40** vs 特性均值 **97.31** => **-0.09%，落在噪声内**（C0 两臂自身就差 0.47）。
   即 ms/轮 56.9 vs 57.0，**没有变化**。
   代价：decision per_call 4.3 -> 5.0 us（键内联多一次查表），全臂约 +87 ms，可忽略；图对象数量增加。
3. ⇒ **撤销 E5 的『direct 20.6 次/call x 281 us = 5.8 ms/call = 19 ms/轮』**。
   该推算隐含假设『direct 是串行主机工作且在关键路径上』；实测删掉约 0.99 s（占 14.5 s 臂时的 6.8%）**一点没省**
   => direct 启动被别的东西盖住了（GPU 执行或其它主机阻塞），**计数器的增减不能当墙钟的代理**。


## ★ 更正（R190，读全量逐-prompt 数据后）：不是“性能为零”，而是“中位数不变、两个 prompt 有改善”
子代理全量回报里的逐 prompt ms/轮（顺序 c0a -&gt; c0b -&gt; fa -&gt; fb）：
| prompt | C0 两臂 | 特性两臂 | 差 | tg |
|---|---|---|---|---|
| p1 (AL 5.55) | 57.067 / 57.343 | 57.299 / 57.214 | **0.0%** | 97.63/97.16 vs 97.24/97.38 |
| p2 (AL 4.22) | 53.969 / 54.068 | **53.293 / 53.365** | **-1.3%** | 78.62/78.48 vs 79.62/79.51 |
| p3 (AL 6.38) | 56.670 / 56.840 | **54.206 / 53.913** | **-4.6%** | 112.13/111.79 vs 117.22/117.86 |

臂内离散 0.3-0.5%；p3 的组间差 4.6% 约为它的 9 倍，且两个特性臂互相吻合（54.206 / 53.913）=&gt; **p3/p2 上很可能是真效应**。
MEDIAN_TG 取中位数（=p1）=&gt; 头条不动，掩盖了这一点。

**修订后的结论**：
1. 机制成立（direct -20.4%、replay +3.6%）；
2. **中位数指标无变化**（p1 完全不动）；
3. **p2/p3 上有 -1.3% / -4.6% 的改善，待确认**（本轮每臂只有 1 次；需要 ≥2 次重复 + 交替顺序 ABBA 才能定论）；
4. 若确认，则该项**应当采用**（代价仅 +0.7us/compute），并在长 AL 场景（用户真实 agent 场景 AL 通常更高）里更值钱。

**下一轮的最小确认实验**：ABBA 顺序跑 4 臂（c0 / fa / fb / c0），只看 p3 与 p2 的 ms/轮；若 4 个特性点全部低于 4 个对照点，则采纳。

## 对模型的影响（必须改写的认知）
- [META] total 10.66 ms/call x 832 = 8.87 s（臂时 14.5 s 的 61%）曾被我当作『主机就是瓶颈』的证据；
  但删掉 1 s 主机工作零收益 => **这部分主机时间与 GPU 重叠**，主机多半在等 GPU（launch/同步点），不是纯串行前驱。
- 因此两阶段模型的『主机 20.6 + GPU 串行 34.5 可加』**需要用新证据重新校正**；在拿到『同一臂里同时测主机与 GPU』的证据前，
  任何『省主机 X ms/轮』的推算都**不得直接当收益引用**（这条与 E5 的教训同源）。

## 处置
- **不采用**该改动（机制验证完毕、无收益、且有 +0.7us/compute 与图对象增多的代价）；源码已还原，机器已复原。
- 上游坐标不变：#28652（OPEN，问题现象）、#28666（关未并，唯一改 key 的尝试）、#18934/#19754/#21611（key 与 warmup 门的来源）。
- ⇒ 通往 150 的下一步**不能再从『省主机时间』推**，必须换判据：优先做**能直接减少 GPU 串行时间或减少每轮 decode 次数**的项
  （fused draft 单 decode、P-B draft 算子融合、N1/N5 等 kernel 侧），并把『主机侧』的每一项都先在**同一臂内**量出它对轮时的边际影响再投入。