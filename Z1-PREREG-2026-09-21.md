# Z1 先验预测（Round 146，**在看到结果之前写**）

> 目的：Z1 的斜率一旦出来，要有**事先约定的判据**，而不是事后讲故事。本文所有字节数都是**可复算的算术**。
>
> ⚠️ **R148 修正（在看到 Z1 结果之前）**：联网搜到第 8 个参考项目 `jackinthebox52/qwen38-v100-serve`，其补丁说明
> **现役的 `flash_attn_ext_vec` 已经打包 2 个 Q 头（ncols2=2）**，所以 GQA ratio 6 的 KV 重读冗余是 **3 遍，不是 6 遍**
> （该补丁把 ncols2 提到 3，实测 128K KV 流量 26.37 -> 8.59 GB/token = **恰好 /3.07**，与"3 遍"完全吻合）。
> 因此下表的 VEC 预测从 204 KiB 改为 **102 KiB**。原始判断保留在案，不删。
>
> ⚠️⚠️ **R150 第二次更正（仍然在 Z1 出数之前）：上面那次改动是错的，现恢复为 204 KiB。**
> 子代理用**我们自己的代码**逐行核对了来源，结论是：
> - 我们的 VEC 模板是 `template<int D, int ncols, ...>`（`fattn-vec.cuh:19`），且**每一处发射都是 `launch_fattn<D, cols_per_block, 1>`**（`fattn-vec.cuh:541`）
>   ⇒ **ncols2 恒为 1** ⇒ `ntiles_z_gqa = ceil(gqa_ratio / ncols2) = 6`（`fattn-common.cuh:1092`）⇒ **6 遍冗余**。
> - n_q=8（我们生产 verify）走 TILE，`fattn-tile.cuh:1309` 的 `use_gqa_opt && gqa_ratio % 2 == 0` 给出 **ncols2=2** ⇒ 3 遍。
> - **F16 与 q8_0 完全一样**：Volta 分支根本不看 KV 类型。
> - 而 qwen38 那个补丁**只改了 `Q->ne[1] == 1` 的 VEC 分支** ⇒ **对我们生产的 8-token verify 路径没有任何作用**。
> - 另外：该仓库**没有任何 benchmark 脚本/日志/trace**，README 里所有数字**在仓库内不可复核** ⇒ 按我们自己的证据分级只能是 **C**，不是 A。
> ⇒ 所以要动的是 **TILE 的 ncols2（2 -> 3 或 6）**，不是照搬 VEC 的补丁。新节点 **N8T**。

## 1. 先分清三条路径 —— 这条最容易被搞混

| 路径 | 什么时候被选中 | 会不会把 KV 反量化成 f16 | 同一 KV 头被读几遍 |
|---|---|---|---|
| **TILE** | `Q->ne[1] * gqa_ratio_eff > 16` | **会**（`need_f16_K/V = true`） | 一个 tile 内共享，按 1 遍算 |
| **VEC**（q8_0） | `Q->ne[1] <= 2`（量化 KV） | **不会**（q8_0 是 VEC 原生支持类型） | `ntiles_z_gqa = gqa_ratio = 6` => **可能 6 遍** |
| MMA_F16 | `Q->ne[1] * gqa_ratio_eff > 16` 且 cc>=80（**我们 sm70 走不到**） | 会 | - |

**关键推论**：我们 8-token verify（q=8，eff ratio 2 => 16 <= 16）走 **TILE**；
而 `--spec-type none` 的普通 decode（q=1）走 **VEC**。
=> **Z1 的无投机臂量的是 VEC 的斜率，不是 TILE 的**。要量 N1（TILE 去反量化）必须**另加一条 DFlash2 臂**（已通知 Z1 代理加第 4 臂）。

## 2. 每 context token 的 KV 字节（算术，可复算）

几何：`block_count=65`、`full_attention_interval=4` => **16 层全注意力**；`head_count_kv=4`；`key_length=value_length=256`；q8_0 = 32 值/34 B = **1.0625 B/值**

```
每 context token 的 KV 元素数 = 2 (K,V) x 16 层 x 4 KV 头 x 256      = 32,768 个
q8_0 存储                      = 32,768 x 1.0625 B  = 34,816 B = 34.0 KiB
f16 副本（单向）               = 32,768 x 2 B       = 65,536 B = 64.0 KiB
TILE 每 token 每调用流量       = 读 q8 34 + 写 f16 64 + 读 f16 64   = 162 KiB
VEC 若 6 遍冗余真实落 DRAM     = 6 x 34                            = 204 KiB   （R150 恢复：见下方第二次更正）
TILE(n_q=8) 读 q8 + 写 f16 + 3 遍读 f16 = 34 + 64 + 3x64            = 290 KiB   （R153 修正：3 遍是走 f16 副本，不是 q8）
  若 use_gqa_opt 不成立(ncols2=1)则 6 遍读 f16: 34 + 64 + 6x64      = 482 KiB
VEC 若 L2 吸收掉冗余           = 1 x 34                            =  34 KiB
```

对照权重流：Q8_0 模型 27.04 GiB = **29.0 GB/轮**（TP3 => 每卡 9.67 GB）。

| ctx | KV(q8_0) 总量 | 占 29 GB 权重流 | 含 TILE 反量化往返 | 占权重流 |
|---:|---:|---:|---:|---:|
| 8K | 285 MB | **1.0%** | 1.36 GB | **4.5%** |
| 32K | 1.14 GB | 3.9% | 5.44 GB | 15.8% |
| 128K | 4.56 GB | 13.6% | 21.8 GB | 43% |
| 256K | 9.13 GB | 24% | **43.5 GB** | **60%** |

## 3. ★ 由此得到的一处重要更正（改我上一版计划）

**N1 不是 8K 目标的贡献项。**

- 8K 下 KV 全部流量（含反量化往返）只有 **1.36 GB/轮**，摊到 3 张卡约 0.45 GB/卡；
  即便按 800 GB/s 最乐观算，也只值 **约 0.6 ms/轮（1%）** —— 我上一版写 `-3 ~ -13`，是直接把「整条 KV 每轮反量化」当成了整轮成本，**既没除以卡数、也没和 29 GB 权重流对比**。
- 所以：**150 t/s（8K 协议）的路径是 N6 + P-B + N7（全是主机侧）**；N1 改判为**长上下文项**：
  256K 下反量化往返 **43.5 GB/次 verify**（占该轮流量 60%），这才是 256K decode 34 t/s 的头号嫌疑。
- 不变的部分：N1 仍是 **E5 使能链的源头**（手写 mma 的最便宜实证、N8/LOWBIT/N3-decode 的前提），只是**不该拿它去凑 8K 的 150**。

## 4. 判读表（Z1 结果一出来直接对号）

| 观测 | 结论 | 接到 |
|---|---|---|
| 无投机臂斜率 ≈ **204 KiB/ctx-token**（按实测带宽折算） | VEC 的 6 遍冗余**真的落 DRAM** | **N8-vec 成立且价值大**；K2 判为「冗余流量」 |
| 无投机臂斜率 ≈ **34 KiB/ctx-token** | L2 吸收了冗余（或 gqa 打包已生效） | **N8 价值有限**；K2 判为「低效」 |
| DFlash2 臂（128K）斜率 ≈ **162 KiB/ctx-token** | TILE 每轮反量化 **confirmed** | **N1 的长上下文价值成立**（256K 的 43.5 GB 可削到 9.1 GB） |
| DFlash2 臂斜率 ≈ **34 KiB/ctx-token** | 没有发生整条 KV 反量化（GAP §7.1 的链路判断有误） | **N1 的动机需重新论证**；N2 的诊断结论要改 |

## 5. 实测第一批（Round 153）：8K 无投机臂

```
prompt1 tg=44.54   prompt2 tg=45.31   prompt3 tg=45.32 tok/s   MEDIAN_TG=45.31
=> 22.07 ms/token（无投机时 1 token = 1 步）
[RT] perf: n_ctx=8192 rounds=521 | alloc_us=315434 (605 us/轮) enqueue_us=6403011 (12.29 ms/轮)
greedy sha256 = 69207026ca43b19f5df2871a658d9f88bf3c583387a39d4a5ecceb8b21eaa147（本配置专用，不可与 f3edac19 比）
臂内离散 44.54-45.32 = 1.7%
```

### ★ 由此校准出一个影响**所有**带宽推算的常数

```
每 token 每卡要读的权重 = 27.04 GiB / 3 = 9.67 GB
实测 22.07 ms/token  =>  有效带宽 = 9.67 / 0.02207 = 438 GB/s
```

- 我们计划里一直用 **800 GB/s** 当 roofline ⇒ **所有基于带宽的估算都要 ÷ 1.82**。
- 这个 438 GB/s 与从 qwen38 数字反推的 ~434 GB/s **独立吻合**（26.37 GB / (1/16.45 s)），两个来源一致 ⇒ 可信。
- 含义：V100 在这个负载下的**实用带宽只有峰值的 55%**。q8_0 权重流的解包 + 小 M 的非合并访问是主因（不是我们之前假设的"已到 roofline"）。
- ⚠️ 推论：**"权重流 12.1 ms 已到顶"这个旧结论要重审** —— 它按 800 GB/s 算的；按 438 GB/s，权重流其实是 **22.1 ms**，正好吃掉实测的整个 22.07 ms/token（KV 那部分被别的开销掩盖或与权重重叠）。

## 5.5 ★★ 第二批实测（Round 157）：32K —— **零斜率，短/中上下文证伪了 KV 流量模型**

```
8K  无投机: 44.54 / 45.31 / 45.32  => MEDIAN 45.31 = 22.07 ms/token
32K 无投机: 44.50 / 45.25 (第三点待出) => 约 22.27 ms/token

8K -> 32K 增加 24,576 个 context token，每 token 只增加约 0.2 ms => 斜率 ~0.008 us/ctx-token
```

| 假设 | 预测的斜率 | 8K->32K 应增加 | 实测 |
|---|---:|---:|---|
| H2 VEC 1 遍（34 KiB/ctx-token @438 GB/s） | 0.0795 us | +1.95 ms/token | **+0.2 ms** |
| H3 VEC 6 遍（204 KiB/ctx-token @438 GB/s） | 0.477 us | +11.7 ms/token | **+0.2 ms** |

**两个都被证伪，实测比最乐观的模型还小一个数量级。**

> WARNING - **R160 作废声明（自查发现）**：下面整段 H4 与 128K 判据**全部作废**。
> 原因：harness 的三个 prompt 只有一句话（约 100-200 字符），`--ctx-size` **只分配 KV 不填充**，
> 所以 8K/32K/128K 三臂实际都在 n_kv 只有几百的**同一条件**下 decode => **零斜率是测量设计的假象**。
> 正确的深度测试：`llama-bench -d <depth>`，或先用长 prompt 填满 KV 再测 decode。
> 教训：**「改了哪个参数」不等于「那个参数真的起作用了」** —— 上机前必须确认实验条件真的被改变。

### 新假设 H4（作废，仅存档）：KV 流量被**隐藏在权重流的富余带宽里**

```
我们只用到 438 GB/s（实用约 800）=> 还有约 360 GB/s 的富余
32K 时 KV 流量（6 遍）= 6 x 32K x 34 KiB = 6.7 GB/token 总量 = 2.2 GB/卡
  若按带宽计应为 5.1 ms/token —— 实测增加 0.2 ms => **被完全隐藏**
=> 结论：8K-32K 的 decode **不是 KV 带宽受限**，KV 读与权重流并行掉了。
```

### 由此对 128K 臂的**预先约定判据**（这是 Z1 最有价值的一点）

| 128K 实测 | 结论 |
|---|---|
| 约 **22.3 ms/token（~45 t/s）** | KV 仍然被隐藏 => **N1/N8T/N8-vec 在 128K 以下全部无价值**，它们只在 256K+ 才有戏 |
| 约 **42.4 ms/token（~24 t/s）** | KV 开始显形（带宽饱和）=> 需要算饱和点，但 N1 的收益上限由此可测 |

## 6. 复算命令

```
python3 /root/timings.py z1-8k z1-32k z1-128k        # 无投机臂（VEC 路径）
python3 /root/timings.py z1-128k-spec               # DFlash2 臂（TILE 路径）
# 无投机：每 token ms = predicted_ms / predicted_n
# 有投机：rounds = draft_n / 7 ; ms/轮 = predicted_ms / rounds
# 斜率 = (每 token ms[ctx_hi] - 每 token ms[ctx_lo]) / (ctx_hi - ctx_lo)
```
