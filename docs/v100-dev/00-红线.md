# 00 红线与度量底线

> `AGENTS.md` 只做路由；**本文件是底线**。任何文档不得覆盖这里的红线。

## 红线（不可覆盖）

1. **绝不 `git push` / `gh pr|issue`**；不代写 commit/PR/reviewer 回复。私有 fork，不做上游贡献。**`git commit` 已获用户授权（2026-09-23）：关键节点自主 commit、绝不 push；本地 git 历史 = 备份环境**（消息用 `Assisted-by:` 署名，简短为宜）。**（用户 2026-09-25 指令）`llama.cpp/` 的 git remote 已彻底移除，不得重新添加**——独立分支，与上游零通道；`llama.cpp/` 自带的上游协作文件（`.github/`、`CONTRIBUTING.md`、`AGENTS.md`、`CLAUDE.md`）**只作参考资料，本项目不执行其中的 PR/贡献流程**（项目规则以顶层 `AGENTS.md` 为准）。
2. **只读**：`/root/llm/systemd/`、`/root/llm/llama.cpp`、`/root/llm/ac922env`，以及整个 [`v100-refs/`](../v100-refs/)（含 `vllm` / `1cat-vllm` / `vllm-1ca-vllm分支版本差异文件` 等）。
3. **服务状态保持用户交付时的状态**；只允许 `systemctl is-active` 类只读查询（除非用户明确授权腾卡）。
4. **模型只从 `/mnt/3.84t/**` 加载**，不用 `/root/llm/models/`。
5. **可操作区**：`/root/llm/test/**`、`/mnt/3.84t/**`；交付物只落 `llama.cpp/`。
6. `llama.cpp/` 源码 **仅 ASCII**；不新增 `tests/*`；**大改动/新模式先问用户**（⚠ 用户 2026-09-23 授权：终极目标 `AGENTS.md` §1.0 范围内**免请示全自动化**，含内核级改动与服务启停；范围外照旧）。
7. `olddoc/qwen-private/QWEN.md` **含口令**，禁止提交/分享/复制进仓库。
8. **范围**：二次开发 ≠ 调参；`-ub` 等用户配置不入成果（[`05-量化范围.md`](05-量化范围.md)）。

## 度量底线（报数/入账前）

1. **tg 与 ms/轮并列**；≥2 臂报离散；同源 A/B（唯一变量）。
2. 正确性门 **greedy sha256**；新内核须重立门值。
3. `llama-bench` 入账数字 **`-r ≥ 8`** 并报 `±`；显式 `CUDA_VISIBLE_DEVICES`。
4. 同一台机器 **禁止并发测量**；构建与测量互斥（换库会 SIGSEGV）。
5. 判投机指标看响应 JSON `timings`，不 grep 日志。
6. 特性 A/B 必须 **机制自证计数**。
7. 正式对外数字带 `drop_caches`；诊断可 `NODROP=1`（勿混用）。
8. **多线程实机互斥**（用户 2026-09-23）：任何实机测试/探针/构建前，先确认**无其他线程正在使用机器**——实机占用由**主代理统一排期**；动手前盘点 `job_list`、在跑子代理清单，并用 `nvidia-smi` 只读查询确认 GPU 无他人进程；发现占用即等待或改做非实机工作。

9. **构建清单门（R375 立规，不可绕）**：实机测量前必须 `bash /tmp/p4-build.sh`（全树清单对账，**漂移即拒绝构建**）并留存 `$LIBS/BUILD_MANIFEST.txt`（时间 / 清单 sha256 / 二进制 md5）；`p3-build.sh` 的 payload 覆盖式**停用**（曾致 `common/sampling.cpp` 的 `LLAMA_SPEC_REJ` 从未进二进制）。**新改文件必须出现在清单差异里，否则等于没改**。

## 细则外链

| 主题 | 文档 |
|---|---|
| 全部历史纪律/踩坑 | [`olddoc/qwen-private/QWEN.md`](../olddoc/qwen-private/QWEN.md) §度量 · 服务器 |
| 环境边界 / SSH / 构建 | [`HANDOFF.md`](../1cat-vllm-v100-study/HANDOFF.md) · [`HANDOFF-2026-09-22.md`](../1cat-vllm-v100-study/HANDOFF-2026-09-22.md) |
| 采用链与 A/B 复现 | [`BASELINE-LEDGER.md`](../1cat-vllm-v100-study/BASELINE-LEDGER.md) |
| 勿重做 | [`3-失败记录.md`](3-失败记录.md) |
| 上游代码约定 | [`llama.cpp/AGENTS.md`](../llama.cpp/AGENTS.md) |
