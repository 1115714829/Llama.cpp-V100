# P-P0 判读规则预登记（数据落地前立此存照，2026-09-23 17:31 server time）

战役 PID 293950 仍在跑（pp8192 TP3 首行 1114.27 已出，pp32768 进行中）。以下判据在看到任何 32K/131K 数据之前写下。

## 1. M1（KV 头 1/1/2 失衡）确认判据
- **主判据（趋势）**：TP4/TP3 加速比必须随 FA 占比单调上升：ratio(8K，FA≈6%) < ratio(32K，FA≈19%) < ratio(131K，FA≈48%)。
  - 单调上升 => **失衡效应确认**，量级 = ratio(131K) 相对 ratio(8K) 的超出部分（attention 因均衡多拿的收益）。
  - 三档基本持平（差 <3%，即漂移带内） => **M1 证伪**（attention 未因第四卡获得超额收益），入失败记录只改灰。
- **辅助证据**：ratio(131K) 若 ≥1.55（失衡模型上限 1.60，扣跨 NUMA 税前），强确认。
- 已知混淆须写进结论：TP4=(0,1,2,3) 跨 NUMA0→8（HOSTS.md），测得值是**净效应**（已扣税）；TP6 另有 4 KV 头不可 6 分的几何不均。

## 2. R223（TP 扩展性）判据
- pp8192/32768 上 TP4/TP3 vs 理想 4/3=1.333；TP6/TP3 vs 理想 2.0。
- 「明显小于」定义：比值 < 理想值的 85%（即 TP4 < 1.13、TP6 < 1.70）=> TP 开销在吃预填充，写明每卡 t/s 反证。

## 3. 部署建议判据（256K 场景是否 TP4）
- 若 ratio(131K) ≥ 1.30（足以覆盖跨 NUMA 与 AR 税）=> **建议 256K/预填充主导场景用 TP4**，decode 场景维持 TP3（R275）。
- 若 1.20–1.30 => 灰区，报数由用户定。
- 若 < 1.20 => 维持 TP3 全场景。
- 注意 R275：TP4 decode 变差 +3.6% ms/轮 且 AL 65.3%→45.9%，部署结论必须**分场景**。

## 4. 口径声明（写进账本）
- NODROP 诊断口径（页缓存热）；pp-only（-n 0）=> greedy sha256 门不适用（R271 先例）；sweep 臂 -r 8 ±；m1 臂 -r 3 ±（长上下文口径，R272 先例，偏离 -r>=8 须注明）。
- env 恒定：LLAMA_SM70_D256=1 / GGML_GALLOCR_SLOTS=3 / GGML_CUDA_P2P=1；库 = /root/libdir-t8b（md5 = B5 账本值）；唯一变量 = -ts + CUDA_VISIBLE_DEVICES。

## 5. 附注（2026-09-23 18:15，m1 首跑失败后的口径修订——无数据可见，无迁就风险）
- m1 首跑四臂（ub 2048）全部 `failed to decode prompt batch, res = -2`（CUDA OOM，死于 131072 深度 prompt warmup）——**零数据产出**。
- 机制：深 KV 下 kq_mask [n_kv x n_tokens] 按 ub 二次放大（ub2048@131K 约 1 GB/设备）+ T8 槽按图形状逐深度换 key 反复重预留（R282 同族教训：「256K 压测须 ub<=512 + 限槽」）。
- 修订：m1 组改 **-b 512 -ub 512**（臂名 m1r-*），其余（长度 131072、-r 3、镜像序、env、库）不变；跨长度比较仍用 TP4/TP3 比值并注明 ub 口径差（比值无量纲，且 FA/线性份额对 ub 一阶不敏感——U3 仍标未验证）。
