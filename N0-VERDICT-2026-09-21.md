# N0 判决 - 强制同步实验（Round 142，2026-09-21）

> **一句话**：在 target decode 之后**强制插入一次同步，只多花 0.4-0.8 ms/轮（0.65-1.5%）**，
> 且两臂的 `draft_n` / `draft_acc` **逐位相同** ⇒ **target 步在每轮结束时本来就已同步**，
> 不存在"主机跑在 GPU 前面一整轮"的重叠。⇒ **可加性模型（轮时 ≈ 主机 + GPU 串行）成立**，
> R1「省 4.8 ms 主机但轮时不动」**不能用重叠解释**，只能用**代价从主机搬到了 GPU**解释。

## 1. 为什么要做这个实验

GAP-ANALYSIS §11.3 记录了一个直接矛盾：

- **可加性模型**（4 臂 layer A/B）：`轮时 ≈ 主机 + GPU 串行`（55.1 ≈ 20.6 + 34.5），⇒ 省主机时间**应该**省轮时；
- **R1（设备侧 push AR）**：省了 **4.8 ms 主机时间**，**轮时却没动** ⇒ 看起来像主机时间被 GPU 掩盖（重叠）。

两者只能有一个对。判据很直接：**人为把同步点提前**。
- 若本来有重叠（主机提交完就跑，GPU 在后面追），强制同步会把**剩余的 GPU 时间**加进轮时 ⇒ 轮时应当**大幅上升**（数毫秒到十几毫秒）。
- 若本来就同步，强制同步只花"最后一次同步之后新产生的 GPU 工作"⇒ **接近零**。

探针：`LLAMA_ROUND_TIMING_SYNC=1`（env 门控，默认零影响）。

## 2. 结果（A-B-B-A，只用干净的一对 sy1b / sy0b）

| 臂 | EXTRA SYNC | p1 ms | p2 ms | p3 ms | p1 t/s | p2 t/s | p3 t/s | draft_n | draft_acc |
|---|---|---:|---:|---:|---:|---:|---:|---|---|
| **sy0b** | 否 | **5162.602** | **6330.460** | **2641.495** | **98.98** | **80.72** | **115.46** | 642/843/336 | 419/390/258 |
| **sy1b** | 是 | 5196.105 | 6429.469 | 2676.771 | 98.34 | 79.48 | 113.94 | 642/843/336 | 419/390/258 |
| sy1（作废：受污染） | 是 | 5208.686 | 6406.612 | 2672.554 | 98.11 | 79.76 | 114.12 | 同 | 同 |

按 `rounds = draft_n / 7` 折算（`/root/timings.py` 是权威口径）：

| prompt | rounds | sy0b ms/轮 | sy1b ms/轮 | 差值 | 相对 |
|---|---:|---:|---:|---:|---:|
| p1 | 91.71 | 56.29 | 56.66 | **+0.37** | **+0.65%** |
| p2 | 120.43 | 52.56 | 53.39 | **+0.83** | **+1.55%** |
| p3 | 48.00 | 55.03 | 55.77 | **+0.74** | **+1.32%** |

- 两臂 **`draft_n` 与 `draft_acc` 完全一致**（642/843/336 与 419/390/258）⇒ 接受轨迹逐位相同，A/B 严格可比（seed 固定生效）。
- 强制同步的代价 **0.4-0.8 ms/轮**，只占 55 ms 轮时的 **0.65-1.5%**。
- `[RT] enqueue_us`：sy0b **20.09 ms/轮**（5,907,654 / 294）vs sy1b **20.20 ms/轮**（5,940,034 / 294）⇒ 提交窗口也没变。

## 3. 判决

1. **可加性成立**：target 步本来就是"主机做完 -> 阻塞等 GPU -> 下一轮"，
   ⇒ `decode+sync` 里那 ~16 ms（36 ms 总时长 - 20 ms 提交）**是真的在等 GPU**，不是异步窗口。
   ⇒ **省下来的主机时间不会被 GPU 掩盖** —— 这对 **N6（打 53% 的 meta 主机循环）是正面支持**。
2. **R1 矛盾消解**：push AR 把 allreduce 从主机搬到设备，**主机 -4.8 ms 的同时 GPU 增加**，
   在可加性模型下**总和不变** ⇒ "轮时不动"是必然，不是"重叠"的证据。
3. **X3 仍然不能算已证伪**：sglang-V100 报 **one-shot push all-reduce 3.35 us**，我们 R1 在役 61.5 us，
   **差 18 倍** ⇒ 口径（几卡 / 消息大小 / 是否含同步 / 内核时间 vs 端到端）必须查清后重审。
4. ★ **附带发现（可复现性）**：`[RT] target decode+sync` 的每 32 轮窗口值在两臂间**逐位接近**
   （29.464/43.779/32.277/40.164 vs 29.464/43.460/32.307/40.065）⇒ 轮时随状态的**确定性振荡**
   （29~44 ms/轮，±25%）是真实信号而非噪声 —— 归因实验必须按窗口对齐，不能只看总量平均。

## 4. 图上的落点

- **N0** 状态 -> ok（定论）
- `N0 --> ADD`（独立验证）、`N0 --> X3`（裁决）、`N0 --> X4`（据此重审）
- **X3** 标签就地写入 R142 解释，避免以后重复猜

## 5. 原始产物

- `/tmp/sync-ab.txt`（各臂 `[RT]` 原始输出）
- `/tmp/p60-sy0b-{p1,p2,p3,greedy}.json`、`/tmp/p60-sy1b-*.json`（权威 timings）
- 复算：`python3 /root/timings.py sy1b sy0b`
