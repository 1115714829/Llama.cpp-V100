# 吐字侧战役报告（B5 -> B6 之争）2026-09-23

## 一、结论
- **采用链未变：B5 = 45.94 ms/轮**（tg 113.28/94.32/145.65，AL 5.55/4.22/6.38，门值 f3edac19...）。
- F3（meta 子图级 CUDA 捕获）六轮实验（v1-v6）**全部不采用**，真实净值 **0±0.3%**（详见 R281-R285）。
- 硬指标 37.0 未达；剩余头寸全部已定价（见四）。

## 二、六轮 F3 实验表（同源 env A/B、ABBA 0/1/1/0、门值全绿、draft_n/draft_acc 四臂逐位一致）
| 版本 | 机制 | 均值 Δ | 判定 |
|---|---|---|---|
| v1 | 指针签名 | — | 确定性损坏（draft_n=3549 逐位同）|
| v2 | 水槽签名 | +0.3% 降低 | 不采用 |
| v3 | +热身出窗 | +1.2% 降低 | 不采用（一次性成本假说被杀）|
| v4 | 内容签名 | +0.0% | 不采用（recs=12507 地址漂移重录）|
| v5 | +二见才录 | +2.4% 降低 | 不采用（512 线性双扫描税）|
| v6 | +O(1) 直映射槽 | **+0.15% 提升（噪声）** | 不采用；recs=356、p3 税根除 |

科学结论：早期 p1 -2~4% 为「重录开销与回放收益」交叉假象；拉平簿记后捕获净值归零。
稳定信号（供后人）：p1 每版皆 -1~-4%（真），p3 每版 +5~11%（真），互相对冲。

## 三、本战役资产
- 标定尺三把：主机穿透 50%（1 ms/call ~ 1.50 ms/轮）；GPU 真实 duty 70-85%（nvidia-smi 本平台欠采样，须饱和段校准）；[META] 解剖 dev 4.8+ar 2.2+prologue 0.6 ms/call。
- 方法论：机制自证先行；分账先于设计；env 判值不判存在；还原树+影子源双通道 + 标记串三查；record-only 鉴别法；热身请求计量协议（harness WARMUP=1，默认零变化）。
- 技术可复用：内容签名正确性法（data+ne+nb+op+type+src 数据，零对象指针）；二见才录政策；O(1) 直映射槽+槽内 seen 态。

## 四、离 37.0 的剩余头寸（全部已定价）
| 猎物 | 预期 | 状态 |
|---|---|---|
| Fix B：meta 容器乒乓 + 内容键化 | -2~5 ms/轮 | 已定位；**动手前须用户批准**（E6 五次 abort 雷区）|
| F4：量化 matmul 内核段（M≈5-12，W8VOLTA-MMA 设计已有） | -2~3 ms/轮 | 内核工程，量级最大 |
| B2：图内 selector TP 化 | -2~3 ms/轮 | 模型几何锁死（n_embd_dec=32）；换大 hidden draft 即解锁 |
| 微款（topk 分块等） | -0.5 ms/轮 | 随手 |
合计 -6.5~12 => 37.0 在射程内（需落 2-3 件）。

## 五、调参建议单（用户侧启动参数，不占开发）
| 参数 | 建议 | 场景 |
|---|---|---|
| GGML_GALLOCR_SLOTS | 3（默认 8） | 长上下文（256K q8_0 KV）防 OOM：8 槽 x 形状数 x ~1 GB 计算缓冲挤爆显存 |
| --tensor-split | 按卡数等分 | 吐字随卡数单调变差（TP3 最优于本尺）；加卡只买容量不买速度 |
| -ub / -b | 默认 512/2048 | 预填充调优可试 ub 8192（+2.9% PP），显存紧张时反向收紧 |
| GGML_META_SUBGRAPH_CAPTURE | 0（默认关） | F3 存档件，净值 0，勿开 |
| GGML_SPEC_EARLY_PROCESS / GGML_SCHED_POOL | 勿设 | 已证伪件（R280/R279）|
| GGML_GALLOCR_FAST | 1（默认开） | 跳过适配复检，中性偏正 |

## 五点五、F4 计划书（量化 matmul 内核段，预期 -2~3 ms/轮）
- 对象：decode 路径 Q8_0 的 M≈5-12 段（mmvq n-cols 小批量），现走标量式 MMVQ（R237/R244 调参已封顶）。
- 路线：W8VOLTA-MMA 设计（DESIGN-W8VOLTA-MMA.md 已有）：QP 切 N、A-stationary、56 寄存器、主循环无 smem、全程 1 barrier（flash-attention-v100/v100-skinny 战法移植到 GEMM）。
- 梯子：(0) test-backend-ops 定位 M=5-12 段基准；(1) 单形状 microbench A/B（含位级数值门）；(2) 内核接入 mmvq 分派（env 门控 GGML_W8VOLTA_MMA）；(3) p60 四臂 ABBA 采认。
- 工程量：内核重写级（1-2 天）；风险 = 数值等价（累加序）与 smem/寄存器压力调平衡；X11 前车之鉴（WMMA 无 ldmatrix/swizzle、样本曾坏）在案。
- 备选小件（随手）：top-k 扫描两遍法（-0.25~0.35，等值先到者胜规则须保持）、gate 转置 8x8 分块（-0.15~0.2）。

## 六、复现
- 库：/root/libdir-sel2（B5 等价 + F3 门控件默认关）；账本复现行见 BASELINE-LEDGER.md 各 R 条。
- 尺度：p60-ab-harness（3 prompt、NPRED=512、NODROP 诊断口径）；ms/轮 = AL/tg*1000 或 pred_ms/rounds（rounds=draft_n/7）。
