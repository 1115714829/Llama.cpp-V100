# AGENTS.md — 路由层（自动加载）

> 本文件只保留：**① 强制工作项 ② 路径地图**。其余一律外链，勿在此扩写。
> 新会话顺序：本文件 → [`docs/v100-dev/README.md`](docs/v100-dev/README.md) → 按需下表。

---

## 1. 强制项（必须做，不可省）

### 1.0 ★ 终极验收目标（用户 2026-09-23 定，凌驾所有其他目标）

> **折腾 llama.cpp 的唯一意义 = 追平 1cat-vLLM。否则不如直接用 vLLM。**

| 项 | 定义 |
|---|---|
| **标准线 BL1** | 1cat-vLLM 标准服务（`vllm-1cat.service`：FP8+DFlash2、TP4、**基本启动参数**）的 **256K 压测实测值** = 极限目标 = 硬件能力证明。实测后填入下表 |
| **追平判据** | llama.cpp 在同口径 256K 实际负载下（同 prompt/采样/请求形状，见 `.dsh/skills/stress-256k/`），**全面能力 ≥ BL1：吐字（tg t/s）、预填充（TTFT/pp t/s）、实际使用直观体感（tpot / ms 每轮 / 响应流畅度）** |
| **显存包络（不合格线）** | 总显存 ≤ vLLM 部署的 **4×V100-16GB**（约 64 GB）；**用更少卡/更少显存达标 = 优，超过 = 不合格**。**调测阶段不限卡数（用户 2026-09-25）**；**交差必须与 1cat-vLLM 同参数口径跑 llama.cpp，卡数与显存只能少不能多** |
| **调测许可** | 小模型（Q2_K 等）可用于开发/基线/压测调参（内核与运行时收益随全量模型带走）；**验收必须在 Q8_0 全量模型上** |
| **全自动化授权** | （用户 2026-09-23）本目标内**自主决策执行、不再请示**——含内核级改动、构建、服务启停、任务自分配、**关键节点自主 `git commit`（绝不 push；本地 git 历史 = 备份环境）**；唯一约束 = 不偏离本核心目标。硬纪律（其余红线、度量底线、实机互斥）不变 |

| 256K 指标 | BL1 vLLM（标准线） | BL2 官方 llama | BL3 B5 | 我们（目标） |
|---|---|---|---|---|
| 预填充 TTFT / pp t/s | **152.5 s / 1549 t/s**（152.26/152.82） | **不可用**（D7 硬崩） | **289.2 s / 817 t/s**（289.36/289.02） | **≥ BL1** |
| 吐字 tg t/s（附 AL） | **124.6 t/s**（118.0/131.1） | **不可用**（D7 硬崩） | **32.7 t/s**（32.29/33.19；AL 0.28，mean len 2.9） | **≥ BL1** |
| 体感 tpot / ms 每轮 | **≈8.0 ms**（8.47/7.63） | **不可用**（D7 硬崩） | **≈30.5 ms**（30.88/30.04） | **≥ BL1** |

> **BL1 口径（2026-09-23 实测，stress-256k 技能）**：ctx 262144 的 **90.14% 填充**（prompt 236,313 tokens；每 rep 加盐防 prefix cache、句序号防投机刷分）；gen 128；thinking on、chunked prefill 2048、DFlash2、TP4 卡 0,1,3,4；服务**原样启动参数**（`vllm-1cat.service`）。比 1cat 文档 2438 t/s 低是因为那是 chunk 8192 pure prefill 契约——本表是**标准服务基本参数实测**，即标准线本体。
>
> **BL3 口径**：同 BL1 prompt/生成长度、thinking on、DFlash2 n-max 7、**TP4 卡 0,1,2,3**（TP3 深 KV OOM：双 KV 头 rank +578 MiB 分配失败）、`--parallel 1 --ctx-size 262144`（llama 把 ctx 按槽均分）、ub 512、库 `/root/libdir-t8b`。**BL2 = 结构性不可用**：官方 434ddbb 与 b11053 原版均 0.05s 内 D7 硬崩（`ggml-backend-meta.cpp:543`，DFlash2 selector top-k × vocab 切分），B5 的 D7 修复是入场券（账本 R292）。⚠ 合成 prompt 对双方投机都有可预测性红利（横向公平），勿与 1cat 文档真实文本 50 t/s 直接比。

**当前差距分解（R293→R298-P 定案，256K 同口径）**：**预填充 = GPU-bound（GPU 152.6 s = 79%，CPU 21%）**；FA 占 GPU ~60-70%。主刀最终排序：**① P-P3 内核线**（FA 大 GEMM 分解，GPU 侧 +25~30%）→ ② R298 调度线（CPU 小头 15-30 s）→ ③ 吐字轮结构（2.65x）+ 纯步（1.44x）。tg 判据一律用 spec-off 纯步锚。
**最新战果（R297→R317 host 收割链全落袋）**：256K 预填充 **289.2 → 169.5 s（-41%）**（R317、双 rep 169.3/169.8）、纯步 34.3 ms 零回归、门值全绿 ⇒ **距 BL1 1.90x → 1.11x**（152.5 vs 169.5）。机制链：桶化（host 形状税）→ ub2048（GPU 批量）→ 单池+收养（alloc 33→2 s）→ 事件组合拳（wait 457→0 ms）；**剩余差距 = GPU 工时本身（host 侧已到物理边际，气泡 = GPU 影子）**。⚠ ub = 启动调参非开发刀（用户 2026-09-24 红线）。⚠ P-P3 分解版证伪入灰。

### 1.0.1 ★ 吐字线（decode）专项章程（用户 2026-09-25 定；与预填充**同章程**，BL1 = 及格线）

| 项 | 规定 |
|---|---|
| **及格线（2026-09-25 用户复核，R375 生效）** | **硬指标只有两条**：吐字 **tpot ≤ 8.0 ms**（⇔ tg ≥ 124.6 t/s）、预填充 **TTFT ≤ 152.5 s**（pp ≥ 1549 t/s）。**ms/轮 降级为内部诊断量**（换算 `ms/轮 = tpot × AL`；AL 5.9 时 47 ms 即达标、AL 3.4 时 27.2 ms 达标）—— 27.2 ms 是 BL1 的派生值，**不得单独当不及格判据** |
| **预填充口径（R375 修正）** | **必须按 spec-on（实际加载 DFlash2）比**：我方 **175.2 s**（2026-09-25 当日 **22 个 spec-on rep 的 TTFT 全部落在 174.0–177.4 s**，pp 1332-1358；另有 4 个异常 rep 181.7/182.3/200.3/340.1 s，未查因）vs BL1 **152.5 s** = **1.15x，未达标**；缺口 ~23 s = 草稿链预填充前向（236K token 过 5 层 1.92B 草稿 ≈ 900 TFLOP，与实测吻合）。**spec-off 的 151.7 s 是纯目标侧数字，不得当作达标证据**（R326 曾误用） |
| **BL1 吐字标准值** | 2026-09-23 `stress-256k` 实测（`vllm-1cat.service` 原样启动、TP4 卡 0,1,3,4、DFlash2、ctx 262144 90.14% 填充、gen 128、temp/top-p 同我方）：tg **124.6 t/s**（118.0/131.1 两 rep）、tpot **≈8.0 ms**（8.47/7.63）、ms/轮 **≈27.2 ms**（= 8.0 ms × AL 3.4，**派生值非实测**）、AL **3.4**（vLLM 不报 AL，系按 ms/轮 反推）。若需重采以锁定标准：起 `vllm-1cat.service` → `stress-256k` 采集 → **恢复停机**（实机互斥三查先行） |
| **预填充代码冻结** | 已落袋预填充代码（T1-C 引擎 `fattn-79t`、79T 路、桶化/ub2048/单池+收养/事件链 R297→R317 采纳链）**不得改动**。唯一例外：**公共组件**（两线共用，如 meta 后端、FA 调度、KV、批构建）确实必须动时**允许动**，但必须 ① 默认行为 = 预填充不变（env 门控 / decode-only）② 改后预填充门值重过（151.7 s ≤ 152.5 s、门 `bcda0092…`）③ **验收标准不变** |
| **显存/卡数包络** | 总显存必须 **≤ 1cat-vLLM 部署包络（4×V100-16GB ≈ 64 GB）**；**超出包络 = 项目宣布无意义**。用更少卡达标 = 优 |
| **原理判据** | 1cat-vLLM 能实现 ⇒ 硬件没问题 ⇒ 差距必是 **llama.cpp 软件代码问题**；不接受"硬件极限"作解释（用户 2026-09-25 重申） |
| **当前吐字差距（R375 复核）** | 256K 同口径：**轮成本才是物理量**（~83–106 ms 稳定），**tg 随 AL 漂移 2.4x**（2026-09-25 的 **23 个 spec-on rep：tg 26.1–63.6，其中仅 3 个 >50**；接受率 23.7–73.8%）⇒ tg 单次读数**不可作 A/B 判据**，报数一律"轮成本 + AL 并列"。对 BL1：tpot 8.0 ms vs 我方 15.7–31.4 ms = **1.96–3.9x**。轮分解与刀序见 [`BASELINE-LEDGER.md`](1cat-vllm-v100-study/BASELINE-LEDGER.md) R374–R375 与 [`PLAN-GRAPH.md`](1cat-vllm-v100-study/PLAN-GRAPH.md) |

### 1.0.2 ★ 构建/测量证据链（清单门 + manifest）

唯一构建树 `/root/llm/test/v100-opt/llama.cpp` 必须与本地工作树**逐字节一致**；一律 `bash /tmp/p4-build.sh`（**漂移即拒绝构建**）；报数必须带 `$LIBS/BUILD_MANIFEST.txt`。**细则 → [`00-红线.md`](docs/v100-dev/00-红线.md) §构建与测量纪律**。

### 1.1 维护 [`1cat-vllm-v100-study/PLAN-GRAPH.md`](1cat-vllm-v100-study/PLAN-GRAPH.md)

1. **先读图再动手**；汇报用节点 id（N0/X5/P-D1…）。
2. **试过的都要上图**；证伪**只改灰、不删**。
3. **每完成一件事回头复读图**：补节点？补粉框条件结论？补边？
4. 外部想法登记进图 **§3** 并连线。
5. 对外交接图：[`docs/v100-dev/4-规划拓扑图.md`](docs/v100-dev/4-规划拓扑图.md)（与上图保持同步）。

### 1.2 比结论 / 报数前先读

| 必读 | 原因 |
|---|---|
| [`docs/v100-dev/00-红线.md`](docs/v100-dev/00-红线.md) | 红线与度量底线（**不可覆盖**） |
| [`1cat-vllm-v100-study/BASELINE-LEDGER.md`](1cat-vllm-v100-study/BASELINE-LEDGER.md) | 采用链 / 唯一对比尺 |
| [`docs/v100-dev/3-失败记录.md`](docs/v100-dev/3-失败记录.md) | 勿重做清单 |

### 1.3 范围（已定）

**V100 二次开发**（代码/内核/运行时）≠ 启动参数调参。主攻格式 **Q8_0** → [`05-量化范围.md`](docs/v100-dev/05-量化范围.md)。

### 1.4 工具与作业习惯（2026-09-24 定；**§1.4.1 等待机制 = 用户 2026-09-25 强制项，其余视情况使用——判断优先，非机械戒律**）

1. **★ 等待必须机制化（用户 2026-09-25 定为强制项，不再是"视情况"）**：远程 shell 一律"**终止符 + 5 秒 sleep 轮询**"。禁止裸轮询、禁止长睡单发、禁止把等待留在对话里。
   - **要等的活**：`bash /root/llm/test/rjob.sh <name> "<cmd>"` 发令后**立即返回**；再用 `bash /root/llm/test/rwait.sh <name> [超时秒] [tail行]` 阻塞——其内部就是 `while ! grep -q JOB_DONE_<name> <log>; do sleep 5; done`，命中即返回，零轮询零上下文；MCP `timeoutMs` 调到 **大于** `rwait` 超时值。
   - **不要等的活**：每条命令结尾**必须回显终止符**（如 `; echo TERM_OK_<name>`）；见到终止符即当轮结束，**绝不二次等待**。
   - 脚本：`1cat-vllm-v100-study/p0-scripts/rjob.sh` / `rwait.sh`（服务器副本 `/root/llm/test/`，均已 `chmod +x`）；日志统一 `/root/llm/test/<name>.log`。
   - `rwait` 超时 = 活还在跑：下一步先 `tmux ls` / 读日志尾，**不叠发第二条命令**；长活仍一律丢 tmux 具名会话。
2. **远程通道**：mcp-ssh 常驻 shell 优先（跨命令状态保留）；`ssh_run.py` 作备用。杀/启**分令**（同命令内 pkill 括号式会被裸名破功）；构建验证与测量启动**两步串行**。
3. **流程**：先读后改（fs 策略）；账本插入用**前缀锚**（old=行前缀，new=内容+同前缀）；证伪**只改灰不删**；测试服务用完即清场。
4. **mcp-ssh 工具守则（工具官方用法，2026-09-24 用户授权并入）**：服务器任务**在 tmux 内进行**（send-keys 发令，MCP 自动回当前 + 上一命令结果）；动手前**先看现有 tmux 窗口**再决定；命令运行中**耐心等待当前命令**（sleep 等待），不并行/后台/叠发下一令；**需要（或可能需要）用户协同时**：建 tmux 具名会话并在**我的回复里**（不在 MCP 内）告诉用户 attach 命令，然后再开工；用户未明确要求时**不建帮助/指南/报告文件**，直接口头回答。

### 1.5 ★ 用户工作规则（2026-09-25，五条）

5 秒等待机制 / 正式环境只读（要改参数先 copy）/ **只做代码优化、不做启动调参** / 无上游通道（可 commit、绝不 push）/ 善用外部资源。**调测不限卡数；交差必须与 1cat-vLLM 同参数口径跑 llama.cpp，卡数与显存只能少不能多。** 细则 → [`00-红线.md`](docs/v100-dev/00-红线.md) §工作规则。

### 1.6 ★★★ 内核根本解决原则（用户 2026-09-26 授权写入本路由文件；**上下文压缩后仍必守**）

> **能从内核根本修的，就不要小修、不要变通、不要绕路** —— 变通来变通去最终可能还是不行；从内核直接解决，痛快、效率高。**一切以彻底解决为基础，不怕步骤多、繁琐。**

| 项 | 规定 |
|---|---|
| **用户端调参（禁碰）** | `-ub`、`--ctx-size`、`-np` 等启动参数 = 用户按服务器设备实际配置，**绝不触碰、不拿调参当成果**；我们只提供**默认值**（如 `-ub` 默认 512 = llama.cpp 官方默认）。与 §1.4.5「不做启动调参」同源 |
| **我们的调优（核心工作）** | **代码级 + 内核级**调优 = 二次开发本体；**内核重写、代码重构都在工作范围内** |
| **难题 vs 小问题** | 小问题（不动内核，如一个边界 bug）= 不是难题，**直接解决**；**涉及内核的 = 难题 ⇒ 一次性从内核啃下**，不许小修变通糊弄 |
| **反例（不许）** | 「FA 25 ms 是物理下限、接受」/「mma 核对 q=8 慢 ⇒ 判负关闭路线绕回 TILE」/「IMA 崩溃 ⇒ 默认关、保守回退」—— 这些是变通回避，必须回到内核彻底解决 |

---

### 1.7 ★★★ 抄配方原则与度量纪律（用户 2026-09-26 定；**上下文压缩后仍必守**）

> **我们学的是做菜的配方、经验、逻辑，而不是把成品菜端过来强行兼容。** 代码片段可以 copy（§1.6④），**前提是它能在我们的形状/数据路上原生跑通**；跑不通就学它的**做法**，用**我们自己的代码或上游既有核**实现同样效果。

| 项 | 规定 |
|---|---|
| **抄什么** | ① **配方**（为什么这样做：并行/融合/放置/显存路径的逻辑）② **可原生复用的代码**（接口与形状天然对得上——先例：预填充 T1-C/79T 引擎直接抄成，**289.2 → 169.5 s** ✓） |
| **不许抄什么** | 把外来核搬进来、再写胶水去"适配"我们的形状。**W1 判例（2026-09-26）**：移植 1cat grouped-verify 核 + 自造暂存/逐 KV 头/自算 `n_parts` 胶水，约 20 轮后**数值仍错、从未过门**，而该线全部剩余头寸只有 −2~8 ms/轮 ⇒ **冻结不删、不再投入**（§1.6③ 判负≠关闭） |
| **度量纪律** | 逐算子分账**只认官准口径**：`test-export-graph-ops` 导出真实形状 → `test-backend-ops perf --test-file`（R212/R203 配方）或差分口径。**`GGML_CUDA_OP_TIMING` 已判不可靠**（见 [`7-吐字线-1对1对比与问题清单.md`](docs/v100-dev/7-吐字线-1对1对比与问题清单.md):52/65），只可当粗筛 |
| **顺序纪律** | **先正确、后速度**：内核级随机值对参考（容差 + 逐位比）→ 集成 → **greedy sha 门值** → 才谈速度。**任何未过门的速度数字一律作废**（W1 的 1.27x 即此例） |
| **调测回路** | IQ1_S（`/mnt/3.84t/Qwen3.8-27B-GGUF/Qwen3.8-27B-UD-IQ1_S.gguf`，5.77 GiB）**单卡 8–32K** 做**相对增速**迭代（§1.0 调测许可）；**绝对值不外推**，验收仍回 **Q8_0 256K TP4** |
| **首选落点** | 能改"自己家"的核就不引外来核：项目已立项同款（如 [`M5-FA-VOLTA-TABLE.md`](1cat-vllm-v100-study/M5-FA-VOLTA-TABLE.md)：给 `fattn-mma-f16.cuh:113-126` 扫 Volta D=256 配置表，量具即 `test-backend-ops perf`） |

**对比机制读哪里**：1:1 对照与施工序以 [`docs/v100-dev/7-吐字线-1对1对比与问题清单.md`](docs/v100-dev/7-吐字线-1对1对比与问题清单.md) 为权威（实读 `v100-refs/1cat-vllm`）；**先读它，再按它的施工序动手**，不要凭记忆复述。

---

## 2. 路径地图

| 路径 | 是什么 | 怎么用 |
|---|---|---|
| [`docs/v100-dev/`](docs/v100-dev/README.md) | **当前有效文档（3+1+范围）** | 对外/决策只认这里 |
| [`1cat-vllm-v100-study/`](1cat-vllm-v100-study/) | 过程档案、账本、E 系列判决 | 证据与细节；冲突以 `docs/v100-dev/` 收口 |
| [`llama.cpp/`](llama.cpp/) | **交付物**（二次开发代码） | 改码前读 [`llama.cpp/AGENTS.md`](llama.cpp/AGENTS.md) |
| [`models/`](models/README.md) | 本机参考模型（vocab GGUF） | 大权重在服务器 `/mnt/3.84t/**` |
| [`olddoc/`](olddoc/README.md) | 旧私有/散件回档 | `qwen-private/QWEN.md` **含口令，禁止外传** |
| [`v100-refs/`](v100-refs/) | **全部外部参考**（已收拢，只读） | 见下表 |

### `v100-refs/` 内（一律只读、禁止改）

| 子目录 | 含义 |
|---|---|
| [`vllm/`](v100-refs/vllm/) · [`1cat-vllm/`](v100-refs/1cat-vllm/) | 官方 vllm 与 1cat 分支 |
| [`vllm-1ca-vllm分支版本差异文件/`](v100-refs/vllm-1ca-vllm分支版本差异文件/) | **分叉点基线**（原 `vllm-forkpoint`）= 1cat 起分支时的 vllm；对照此目录看 **1cat 相对分叉点改了什么** |
| 看差异的命令 | `git -C v100-refs/1cat-vllm diff 4ff865c38..HEAD -- <path>`（分叉点在 1cat 对象库内） |
| **★ 轮子清点/取用指南（抄外部代码前必读）** | [`docs/v100-dev/6-外部轮子清单.md`](docs/v100-dev/6-外部轮子清单.md)（v100-skinny 量化教训、sm70-attn 可抄件、flash-attention-v100、各 fork 一句话索引） |
| 其余 | `flash-attention-v100/` `sm70-attn/` `v100-skinny/` `ninfer-v100/` `jusko-…/` `sglang-V100/` `xllama.cpp/` `qwen38-v100-serve/` — V100 内核/服务参考 |

### 研究档案深链（按需）

| 需要 | 打开 |
|---|---|
| 可信度 / 旧结论更正 | [`AUDIT-2026-09-20-dsh.md`](1cat-vllm-v100-study/AUDIT-2026-09-20-dsh.md) |
| 当下实况交接 | [`HANDOFF.md`](1cat-vllm-v100-study/HANDOFF.md) · [`HANDOFF-2026-09-22.md`](1cat-vllm-v100-study/HANDOFF-2026-09-22.md) |
| 吐字战役报告 | [`DECODE-CAMPAIGN-REPORT-2026-09-23.md`](1cat-vllm-v100-study/DECODE-CAMPAIGN-REPORT-2026-09-23.md) |
| **外部轮子/量化经验（写内核、改引擎、抄代码前必读）** | [`6-外部轮子清单.md`](docs/v100-dev/6-外部轮子清单.md) |
| 已突破 / 计划 / 失败 | [`2-已突破`](docs/v100-dev/2-已突破方向.md) · [`1-计划`](docs/v100-dev/1-计划突破方向.md) · [`3-失败`](docs/v100-dev/3-失败记录.md) |

---

## 3. 一句话现状

**终极验收 = 追平 BL1（§1.0）**：256K **tpot ≤ 8.0 ms（tg ≥ 124.6）+ TTFT ≤ 152.5 s（spec-on 口径）**，显存 ≤ 4×V100-16GB 包络。
**总账（R375 复核）**：预填充 spec-on **175.2 s**（1.15x，缺口 = 草稿链前向 ~23 s）；吐字轮 **~83–106 ms**（tpot 15.7–31.4 ms = 1.96–3.9x；tg 随 AL 漂移，不可单点比）。
**注意力线（R435 官准实测**，单卡真实 256K 形状 `Q[256,q,24,1] / K,V[256,262144,4,1]`，`test-export-graph-ops` + `test-backend-ops perf --test-file`，与文档 §0.4 吻合 1.2%）：
q=8/TILE —— F16 **5209 µs @192.8 GB/s**、Q8_0 全量镜像 **7138**、Q8_0 核内直读 **6071**；q=1/VEC —— F16 **1775 @563.6**、Q8_0 **3474 @153.1**。
⇒ ① R376 的 −5.2 ms/轮 **独立复现**（镜像−直读 = 1067 µs/层）② **但 q8_0 直读本身净亏 862 µs/层**（核内解包 > 省下的字节）③ **两侧都只到峰值 10–21% ⇒ 真头寸是并行度，不是 dtype**。
**施工序（以 [`7-吐字线-1对1对比与问题清单.md`](docs/v100-dev/7-吐字线-1对1对比与问题清单.md) 为权威，数据驱动）**：**注意力并行度（M5 路线：改我们自己的 FA 核的配置/切分，非引外来核）→ host 结构（P1/P2）→ GDN 融合（P5，48 层）→ LM head/采样（P4）→ MoE GEMV 形态（P6）**；AL 提升（P8）在每轮成本降下来后重测。
**已作废 / 冻结（§1.6③ 判负≠关闭）**：移植 1cat FA 核的 W1 线（**冻结不删、不再投入**）；「放宽 VEC 门槛」（R435 判负：VEC 无 KV 切分）；把 `GGML_CUDA_OP_TIMING` 当判据（§1.7）。
**纪律**：实机前先过 §1.0.2 清单门 + 门值（**未过门的速度数字一律作废**）；度量按 §1.7 官准口径。详见 [`docs/v100-dev/README.md`](docs/v100-dev/README.md)。
