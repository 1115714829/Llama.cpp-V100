# heavy / nested — not in this workspace meta repo
v100-refs/
llama.cpp/
.dsh/
.mimocode/
.tmpcmp/
*.gguf

# secrets and bulky backups — never commit
olddoc/qwen-private/
olddoc/backup/
# self-versioned archive repo (its own commits are the backup)
1cat-vllm-v100-study/
