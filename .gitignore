# heavy / nested — not in this workspace meta repo
v100-refs/
llama.cpp/
.dsh/
.mimocode/
.tmpcmp/
*.gguf

# secrets and bulky backups — never commit
olddoc/qwen-private/
olddoc/backup/
1cat-vllm-v100-study/  # self-versioned archive repo (commits inside are the backup)
